# Release testing

The single list of what to run, and when. `RELEASE.md` is the release *procedure*; this
is the *testing*.

**Keep this file in step with `package.json`.** Adding a probe without adding it here
means it runs only when someone remembers it. That is not hypothetical: before this file
existed, 21 test scripts were spread across three partial lists — `CLAUDE.md` named 4,
`RELEASE.md` named 4, the release skill named 7, and no list was complete.

Nor was it hypothetical the second time. Three probes — `ui-parity`, `parity`,
`grant-hint` — were never given an `npm` script and so were never on this list, and all
three quietly rotted against ApplicationV1 internals until the AppV2 port. `ui-parity`
alone carries 30 assertions, including the only geometry checks in the suite. **A probe
without a script name is a probe nobody runs.** (Wired up 2026-07-29.)

---

## Why there are two passes

They test genuinely different things, and neither substitutes for the other.

**Before tagging** runs against your **working tree** on `:30000`, through the directory
junction. It catches what you just wrote.

**After publishing** runs against the **downloaded release zip** on `:30001`, in a world
created from scratch. It catches what the *build* did — a file missing from the zip, a
pack that did not get built, a manifest pointing at the wrong version. None of that is
visible from the working tree, where every file is present by definition.

Concrete proof this split matters: `dev:smoke` was asserting that *every* pack in
`game.packs` is non-empty, including world compendia. `world.custom-backgrounds` is
created on demand and empty until a Warden authors a background — so the check failed on
long-lived worlds and passed on fresh ones. Post-release validation always uses a fresh
world, so it could never have caught it. (Fixed 2026-07-28.)

---

## Before tagging

Run from `c:\Users\domin\code\air-bladder`, on the branch you are about to merge.

### Offline — no Foundry needed

| Command | Checks |
|---|---|
| `npm run check:refs` | every compendium reference in every shipped table resolves, and resolves to a document with the *right name*. Also asserts it found **at least `EXPECTED_REFS` (198) references at all** — because this gate reads a schema, and a schema rename turns it into a gate that checks nothing while still printing "passed". That is not hypothetical: v13 renamed the row type `pack`→`document` and replaced `documentCollection`+`documentId` with `documentUuid`, so its `if (r.type !== "pack") continue` skipped every row in every table. 198 references one run, 0 the next, green both times. Raise the constant deliberately when content adds references. **Since 2026-08-09 it also resolves inline `@UUID` links in result TEXT** — the encounter-row convention stores its monster references there, and they fail exactly as silently — with its own floor (`EXPECTED_INLINE_REFS`, 80). Inline links are deliberately NOT name-checked (a label is display wording: "Mountain Lion" links the Tiger on purpose), and any non-`Compendium.air-bladder.*` link in shipped YAML fails outright, because a world-scoped `@UUID[Actor.…]` resolves only in the world it was authored in |
| `npm run check:fields` | pack documents match the data model, **and `system.json` `documentTypes.*.htmlFields` matches the `HTMLField`s in the schemas, both directions**. That second half is a security check, not tidiness: the Foundry server never loads the data models, so an `HTMLField` missing from the manifest is never sanitized — see `dev:sanitize` |
| `npm run check:traits` | the trait-sentence parser |
| `npm run i18n:check` | translation coverage, placeholder/HTML mismatches, stale keys — language files **against each other**; and since 2026-08-02 the CONTENT overlay against `src/packs/`, which nothing gated before. The overlay is keyed on the English SOURCE STRING, so a translation dies by keeping a key nothing asks for any more, and the three ways that happens are separated because only two are defects: a key that is a spreadsheet's CSV quoting of a real source string (**error** — tooling mangling), the same English present under a DIFFERENT namespace (**error** — a rename or type move never carried through; re-key, never retranslate), and English that exists nowhere (**advisory** — editing prose orphans its translation, which is the recorded cost of the scheme). `i18n:extract` has always computed this into `content-stale.tsv`, which is gitignored and which the extractor spent two days unable to write at all — 32 of Malecho's finished Spanish strings shipped dead in that window. Since 2026-08-02 it also models **Foundry's own loader**: a key holding a string that is also the parent of a dotted key (`CAIRN.NUses` beside `CAIRN.NUses.one`) makes `expandObject` throw and the client abandons the WHOLE language file — a world with every label a raw key, from one added line. That is why the plural forms use an underscore |
| `npm run i18n:repair` | not a gate — the **surgical fix** for whatever `i18n:check` reports as mechanically recoverable: a key carrying HTML entities the DOM never asks for, a key a spreadsheet CSV-quoted, a key whose document changed namespace, and a VALUE holding tab/quote debris. Dry-run by default; `--write` applies. It exists because the advice it replaces — "run `i18n:extract && i18n:import`" — was actively unsafe: that drags all ~1,700 overlay entries through the TSV parser to fix a couple of dozen keys, and on 2026-08-07 doing exactly that **corrupted three further values**, one growing from 172 characters to 875 of an adjacent row's spreadsheet text. **A repair that rewrites everything is only ever as safe as the weakest record it passes through.** This one touches only the entries it names, preserves key order so the diff is the repair and nothing else, and keeps every translated value byte-identical |
| `npm run i18n:handoff` | not a gate — it **writes `docs/translation-handoff.md`**, the document the translator is given each cycle, and it is here because the thing it reports is invisible everywhere else. Its first two sections are *work he already finished that we have since changed*, and the two halves of the system lose that work differently: an interface value edited under a finished translation leaves the translation in place, valid and **wrong** (detected against `tools/i18n/baseline/<lang>.json`, which records the English each translation was verified against), while editing pack prose leaves the content overlay's key unasked-for and the entry dead — reachable by nothing, and reported by `i18n:check` in the same bucket as prose we deliberately deleted. Neither is a missing key, so neither falls back to English and neither is visibly untranslated; both look finished. `extract-ui` marked exactly those rows `done` until 2026-08-07. Regenerate it BEFORE the tag, commit it, and send it — a stale one understates by exactly the strings changed since. Run it per locale with `-- --lang <code>`; the unsuffixed file is Spanish because that is the path the maintainer guide and the translator both know |
| `npm run check:probes` | that **every probe under `tools/dev/` is reachable as an npm script**. A probe with no script is a probe nobody runs, and an unrun probe rots silently — it keeps passing in the imagination while testing nothing. This gate exists because on 2026-07-29 there were **18** of them; see "The 18 orphans" below for what they were hiding |
| `npm run check:licence` | that **`LICENSE.txt` has not shrunk back to a bare MIT file**, which is what it was until 2026-07-30 — 21 lines of unqualified MIT over six regimes, naming Yochai Gal as a copyright holder *of the software* and purporting to grant sublicensing over CC BY art, CC BY-SA game text, OFL fonts and an all-rights-reserved logo. `README.md` was right the whole time; the two had simply drifted, and the wrong one is the one that ships in the zip and that GitHub reads for the repo's licence badge. Asserts the file does not *open* as a single-licence template, that the MIT grant is present and scoped to the code, that both files name the same six regimes (README is canonical), that every per-asset notice it delegates to still exists — a rotted pointer there is a broken licence, not a broken link — and, since 2026-07-30, **that every directory the release zip ships is named by some clause**. That last one is the same question asked the other way, and it was blind: `lang/` shipped named by nothing, which the rewrite's own "these and only these" makes an affirmative exclusion rather than an omission, leaving seven interface files (five of them inherited third-party work) with no grant at all. What ships is read from the workflow rather than a list kept in the checker, because a second list drifts exactly as `LICENSE.txt` drifted from `README.md`. Since 2026-08-02 it also compares a NUMBER for the first time: the gallery size stated in both READMEs (part of a CC BY attribution) and in four working-note copies must equal what is on disk. It compared names and never numbers, which is how `1,310` and `1,239` both survived a collection holding 1,366. The patterns are anchored on purpose — a reword that breaks one fails the gate, where a regex quietly matching nothing would put the count straight back to unchecked |
| `npm run check:manifest` | the `system.json` keys that fail **silently**. Chiefly `styles`: until 2026-07-30 it was `["css/cairn.css"]`, the pre-v13 string form, kept alive only by `BasePackage._migrateStyles` — which is `@deprecated` since v13 and emits **no** `logCompatibilityWarning`, so nothing in console, Setup or any probe reported it, and the day the shim is retired every sheet renders unstyled for anyone who installed the zip. Also asserts `esmodules`/`styles` name files that exist, that `readme`/`bugs`/`changelog` are declared and absolute, and that every declared pack has YAML in `src/packs/` (with a count floor, for the same reason `check:refs` has one). Since review #13 it also asserts every warden-* pack plus `macros` carries `ownership` PLAYER/TRUSTED: NONE — the five Warden-facing packs are hidden from players' sidebars, and without the manifest key a new one ships player-visible (base-package.mjs:120 defaults PLAYER to OBSERVER); keyed on pack NAME, not label, because labels are i18n keys. Hiding is sidebar-only — a NONE pack still serves reads (verified live: a player client indexed, fetched and rolled the warden-npcs name table under NONE), so generation's reads into warden-npcs/warden-monsters are unaffected. Takes an optional path argument, which is how it gets negative-controlled: `node tools/dev/manifest-check.mjs <(git show HEAD:system.json)` must fail. The file's header records **where each key actually renders in the shipped 14.365 client**, which is narrower than it looks — `changelog` in the compatibility checker, `readme`/`bugs` only in Module Management (which does not list systems), `media` not at all |
| `npm run check:warden` | every shipped **Warden table row against the live SRD** — 536 rows across 33 tables (the SRD faction suite from Setting Seeds joined 2026-08-02), fetched from `yochaigal/cairn`, plus that each `(See …)` cross-reference names a table these packs actually contain. Anything shipped but unchecked must be listed in `SPEC` or excused in `NOT_SRD` by name, so a new table cannot arrive unverified. It was listed here as "Warden-facing settings", which is not what it does, and that mislabelling is roughly why nobody noticed it had stopped reading rows at all: v13 split `TableResult#text` into `description` (text rows) and `name` (document rows), so all 376 compared as `""` and it reported 15 tables of SRD mismatches — loudly, but pointing at the content instead of at itself. It now fails with a message naming the *schema* when every row reads empty |
| `npm run i18n:source` | the code **against `lang/en.json`**: a key referenced but missing (the user sees `CAIRN.Whatever`), a key nothing references (dead weight a translator is still asked to translate), and user-visible English that never reaches `game.i18n` at all. That last class is invisible to `i18n:check` by construction — an unlocalized string is identical in every language file because it is in none of them, which is how `title="Double click to change limit"` shipped as the only hint that feature existed. A fourth class since 2026-08-04: a **gallery category in a manifest with no label key**. Those labels are built by interpolation (`CAIRN.GameIconCategory.${pascal(key)}`), so the first two classes are both structurally blind to them — the scan records the dynamic PREFIX and stops, and that same prefix licenses every key under it as "used" — while the tile renders the literal string `CAIRN.GameIconCategory.Fire`. Read from the generated manifest rather than a hand-kept list, because a list maintained beside the thing it describes drifts from it; adding a category was the one step of that whole operation nothing gated. **The corpus includes the macros pack's YAML since review #13**: each macro `command` is JS the client executes, and its toasts localize exactly like `module/` does — with the corpus stopping at `module/` + `templates/`, all nine keys referenced only by macro commands sat "unused" and the gate ran permanently red, the exact failure its own docstring names. Only the macros pack is scanned; the other packs hold prose for the content overlay, where an en.json key would be a bug. **A fifth key producer since 2026-08-09: `system.json` pack labels** (review #13 #15). Core localizes `metadata.label` per viewer (compendium-collection.mjs:46), so the 24 labels are `CAIRN.Pack.<name>` keys and the gate scans the manifest in both directions — a key-shaped label is a REFERENCE (missing key → "used but missing", and the 24 keys stop reading as dead), a non-key label is reported as hardcoded (witnessed all three ways: a literal label reds the new class, a key en.json lacks reds "missing", and the gate without this half calls all 24 keys unused). **`packFolders` names are exempt BY MECHANISM, not oversight**: the server writes a packFolder's name verbatim into a real world Folder document at world init (dist/packages/world.mjs, `db.Folder.createDocuments`, matched across launches by dot-joined hierarchyName), nothing localizes a Folder document's name, and one shared document serves every viewer's language — the review's claim that folders share the labels' route was checked against the server and refuted. `dev:companions`'s label leg is the live witness that a key label still renders as English (an unresolved key would read `CAIRN.Pack.mounts-transports`) |

### With Foundry running on :30000

Start the dev server first — it is local infra, not an npm script. These load the working tree through the
junction, so no build step is needed — except `npm run build:packs` if `src/packs/`
changed, which **fails while a world is open**, so stop the server for it.

| Command | Checks |
|---|---|
| `npm run dev:smoke` | system loads, shipped packs non-empty, a character sheet renders, zero console errors |
| `npm run dev:data-model` | the TypeDataModel schemas. Since 2026-08-05 the spellbook row also round-trips `system.glog`, with a discrimination control — one update writing `glog` and a fake `zzNotAField` together, asserting the schema keeps one and drops the other — because a round-trip can only fail for keys the schema DECLARES, so on its own it cannot catch a field that was never added |
| `npm run dev:icons` | no document left on a `.png` icon; every icon 200s, is really SVG, and rasterises at full size. Its list comes from the **`icons/` directory**, not from icons in use by documents — it used to be the latter, so it checked 15 of 17 files and a newly added icon stayed invisible to it until content pointed at one. Since 2026-08-02 it also covers the **picker gallery**: all 1,643 `art/game-icons/` glyphs must declare an intrinsic width/height, read off DISK because 1,643 browser round trips to assert a regex is not worth the wall clock, plus a three-glyph sample rasterised in the browser so the disk reading corresponds to something real. Control: with the stamp reverted the sample renders at **150px**, which is the `stack.svg` symptom exactly |
| `npm run dev:sanitize` | that the server actually strips scripts from a system `HTMLField`. Writes a payload **as a real player** (Alice) to their own character's `system.notes` and to an owned weapon's description, and asserts both come back cleaned with their benign content intact. Needed alongside `check:fields` because the manifest declaration only takes effect at server STARTUP — an un-restarted edit is indistinguishable from no edit |
| `npm run dev:roll-npc` | that Roll NPC **asks first**. It routes an npc to `regenerateNpc`, which deletes every embedded Item and overwrites the statblock, and it used to do that on one click — harmless for a hireling, which is what it was written for, and a one-click no-undo wipe of any of the 205 shipped monsters after the Hireling→NPC fold. Asserts the dialog opens, that declining leaves the statblock *and* `_stats.modifiedTime` alone, and — the assertion that makes the other two mean anything — that accepting really does regenerate. The decline check waits **8s**, because regeneration takes ~5s and an earlier version waited 1.5s and reported the destructive behaviour absent. The seed states `generationEnabled: true` since 2026-08-02 — the default is Off now, and this probe is about what the button DOES, not whether it shows |
| `npm run dev:gen-dice` | that character generation **posts its dice to chat, and posts nothing else**. Generating a PC must produce exactly ONE message carrying five real `Roll` objects in the order HP/STR/DEX/WIL/Gold — that array is the whole feature, because Dice So Nice animates any roll riding a created ChatMessage and core supplies `CONFIG.sounds.dice` without it, so no `game.dice3d` call exists to test. Every roll is **tied to the stat that was stored** (`hp.max`, `abilities.*.max`); without the tie the card could show decorative dice unrelated to the character and still look right. **Gold is an inequality, not an equality** — stored gold is the roll PLUS bond and background-choice gold, so the card shows the bare roll and the assertion is `roll ≤ stored`; asserting equality reds on any background that grants coin. The negative control is the setting itself: with `show-generation-rolls` off, generation must post **zero** messages, or leg one proves only that something posts. Regenerate posts one; the background **Test ×10** preview posts none (it rolls choice tables ten times and would be a twenty-message storm if `evaluateFormula` had been the hook point instead of the two entry points). Last leg is the user's explicit ruling: **name, portrait and background re-rolls never reach chat** — driven by clicking the three real sheet controls, with their presence asserted first, since zero messages from three buttons that never rendered proves nothing. Actors and messages are cleaned from **Node**, reading ids the page stashes on `window` as it creates them, so a throw partway through still tidies up. Last leg (2026-08-06) is **ORDERING**: the dice must finish animating before the generated sheet opens. `ChatMessage.create` resolves on save while Dice So Nice animates for seconds after, so the sheet used to cover its own roll; the leg records the last `diceSoNiceRollComplete` and the first `renderCairnActorSheet` and requires that order. Its control is in-page and is what makes it mean anything — stub DSN's own `waitFor3DAnimationByMessageID` to resolve immediately, which reproduces the pre-fix world exactly (dice still animate, nobody waits) without touching system source, and require the order to INVERT. **If DSN is not active in the world the leg FAILS rather than skipping quietly**, because an ordering it cannot observe is not an ordering it has checked |
| `npm run dev:monster-gen` | the **Generate Monster** button and everything it promises (docs/monster-generation.md). Warden-only both ways: present for the GM, absent for Alice **while her role holds ACTOR_CREATE** — the grant is what makes that leg mean anything, because without it she gets no generator section at all and the assertion passed identically with the isGM gate deleted (caught by this probe's own fail-witness run); her Generate PC button is asserted present as the non-vacuousness witness, and the permission is restored from Node. The tier picker opens with all four buttons and **dismissing it creates nothing** (the issue #6 rule). Standard's whole shape is asserted — role monster, HP 3=3, every ability on the 3/6/10/14/18 ladder with DEX never at an extreme, exactly one starred attack item carrying d6, derived Armor ≤ 3 with armor items drawn from the allowed names, a ProseMirror-canonical `<ul>` description whose critical damage is a **prose** bullet, a game-icons creature portrait mirrored onto a hostile unlinked token with STR/HP bars, and no Connections tab. The 8 Warden tables' drawn state is **delta-checked** untouched (roll(), never draw() — and a world where a human drew on them still passes). The API tiers pin serious→10/d10, hardier→6/d8, random→a real tier. Roll on a monster sheet must re-ask the **tier picker** (monster-worded — the picker IS the confirmation), declining must change nothing after the full 8s, and accepting must replace statblock and items while **keeping name and portrait**. Ends on a negative control: the shape checker fed a corrupted clone must object, or every shape green above is vacuous. The decline leg is also what caught the description-normalization write: a non-canonical description makes ANY frame-button click submit a real diff and rewrite the document. Since 2026-08-02 the sheet leg also asserts a generated monster **lands with Randomization off** (Roll starts hidden; the regenerate legs flip the real toggle first) and that its Roll button reads **"Roll Monster"** wearing the directory button's `fa-dragon`. Needs `npm run dev:players` (Alice) |
| `npm run dev:faction-gen` | the **Generate Faction** button: Warden-only in the Actors directory, one click mints a **JournalEntry dossier** (the system's first) named "The \<Trait\> \<Type\>" from the rolls — membership-checked against the pinned SRD columns, never "non-empty" — with all six sections on one GM-only page, and the sheet opens for editing. The **world-first contract**: a sentinel world `Warden: Faction - Agenda` must land on the page verbatim, and a world `(Count)` copy forced to 4 must yield **exactly four DISTINCT advantages** from the stock twenty (the SRD's roll-count-then-reroll-repeats procedure). Sentinel tables are swept on the way IN too — a prior aborted run's world copy would shadow the pack and corrupt the stock leg |
| `npm run dev:encounters` | **encounter tables** (module/encounters.js, 2026-08-09) — the Warden-authorable convention: a table row whose STORED English text parses (`1d6 @UUID[…]{Goblins}`, `1 random NPC …`) grows a Warden-only "Add to scene" button on its draw card, injected per viewer (a player's copy has nothing to trim). Deterministic draws via constant-formula Rolls (`new Roll("2")` lands row 2 — no leg is dice-decided). Asserts: the button names its work; a fixed `3` row places exactly three NEUTRAL, unlinked, grid-clustered, non-stacked tokens and imports exactly ONE world actor into the flag-marked Encounters folder; a WORLD-actor row with a HOSTILE prototype comes out NEUTRAL — the built-in witness that the disposition is FORCED, which reds alone if the force is dropped — and imports nothing; the stamp refuses a second spawn (spent button is only the affordance); a second draw REUSES the import; the `1d6` row places 1..6; plain text earns no button; the random-NPC row mints one fresh person (folder, statblock, portrait) with a token; Alice driving the export directly is refused unstamped (the isGM differential); and with `canvas.scene` shadowed null IN-PAGE the spawn refuses before rolling. Plants scene/table/actor itself, restores the previously-viewed scene, sweeps everything from Node with names and ids printed |
| `npm run dev:grimoire` | **the item Grimoire and its cast flow** (module/grimoire.js + CairnItem, 2026-08-09) — the resolver as a unit (per-power `[1]`/`[2]` blocks, `[sum]`/`[dice]`/`[dado]` substitution, arithmetic incl. the × spelling, non-numeric brackets untouched, bare-digit markers never eaten — and since 2026-08-10 every resolved value comes back MARKED, a `grimoire-resolved` span with the authored expression in its tooltip, asserted as the exact wrapped shape in the unit legs and on the rendered card for both the page and the bookless-scroll casts; the unresolved-`[sum` check reads the visible text with tags stripped, because the tooltip holds the expression on purpose); transmute via the REAL row control and confirm (bound + weightless, grouped/indented/chipped under the book, slot freed; a scroll comes out a PAGE with scroll off and uses cleared); bound is forever (a direct un-bind is stripped, a hostile multi-field write leaves every pin); the one-book wall BOTH layers independently (drop handler null + `_preCreate` refusal — each witnessed red alone) while a pile takes two books; page capacity gone-affordance + refusing-handler; the cast with **dice SHADOWED in-page** (`CONFIG.Dice.randomUniform` pinned to a sequence — the client maps `ceil((1-u)*faces)`, so a doubles assertion is chosen, not rolled): seeded `[4,4]` proves the public card carries the RESOLVED power-2 block and the whisper goes to the caster alone with the 2-Fatigue button, the Mishap line and a drawn world-first Mishaps row; seeded `[1,4]` proves no-doubles and the `_one` fatigue form; a single-die `[4]` cast proves the dice line's own `_one` form ("Rolled 1 magic die, result is 4" — a lone number must read as a result, never a count; the plural line lists the faces with the sum in parentheses, "result is 4, 4 (8)", and the Add-Fatigue button wears `fa-weight-hanging`, the inventory Fatigue icon, asserted with the battery's absence; wording and icon the user's, 2026-08-10); the Add-N-Fatigue button lands at a FULL pack (`ignoreCapacity`) and the message flag spends it; zero free slots refuse the cast; the power select caps at 4; travel moves the book WITH its pages both directions (witnessed red with the bundle removed) and a page alone is refused; **a SPELLSCROLL casts with NO book** (the hack: destroyed after a single use, otherwise exactly a Grimoire spell) — gated on `enable-glog-magic`, which the probe SHADOWS in-page in BOTH directions (the world's value is the user's, possibly live-flipped; the off-differential must be established, never assumed), the full machinery runs bookless, the cast SPENDS the scroll (witnessed red with the spend removed — and the second-cast leg only fires on a spent scroll, or the defeated state re-opens the dialog and hangs the run instead of redding), a spent scroll refuses with the warning; Alice casts end-to-end with the whisper addressed to her. **Both cast tiles identify themselves in the flavor line (2026-08-10)** — a lit "GLOG" tag plus an em dash opens it (the Die of Fate glow recast in the pinned violet `--ab-glog`; chat tiles are parchment in both themes, so no dark override), then "{name}'s Spell", escalating to "{name}'s spell triggered a magical mishap!" on doubles — and a no-doubles whisper SAYS "No doubles = no mishap" outright (the equals-sign wording is the user's, 2026-08-10), the line asserted absent when a mishap happened. Witnessed red in two defeats (flavor emptied; tag + no-mishap line removed), each redding exactly its own legs. **The book fixtures plant under a settings-read shadow forcing GLOG OFF** (since 2026-08-10): the legs exercise a 2e world's setting-independent mechanics, and in a GLOG world the create seam would convert the planted books to weightless scrolls — the freed-slot leg went 2 → 2 the day the seam landed. **The cast whisper is selected by its `grimoire-cast-whisper` container, never as "the first whispered message"** (2026-08-10): the change-log posts its own [GM + owner] whisper asynchronously from the fixture's item edits (the caster is Alice-owned), and a debounced one landing inside the cast's message window fed the doubles legs the wrong document — a race, redding one cast in some runs and crashing the fatigue leg downstream. Needs Alice (`dev:players`); sweeps its actors, items and messages from Node |
| `npm run dev:role-migration` | the `role` re-stamp, in both of its layers. **The shim** (`NpcData.migrateData`) is asserted on unsaved constructions: `inanimate` → transport/container by class, else npc; **a Kind ALONE must NOT derive** (the fix of 2026-07-31); and since the hireling-role collapse, a stored `role: "hireling"` must read **npc + forHire**, because that value is no longer in the enum and nothing else stops it failing validation on load. `forHire` must no longer STEER the role either — with `inanimate` and a cart Kind beside it the cart wins, where the old early return quietly turned a pre-roles cart into a person. `migrateData` runs over update DIFFS as well as whole sources (measured: `migrateData({containerClass:"pile"})` comes back carrying `role:"npc"`), so a guard of the shape "field X is absent" fires on every update that simply did not mention X — hence the collapse shim keys on the LITERAL value. **The migration** (`migrateNpcRoles`) selects on **nothing at all** and re-stamps every npc-typed world actor blind, which is the only thing that works: `migrateData` rewrites `_source` during construction and `cleanData` PRUNES unknown keys out of it, so neither a stored "hireling" nor a legacy `inanimate` can be seen from a client. Its predecessor selected on `"inanimate" in _source.system` and therefore matched **nothing, ever** — it ran, logged nothing, set its marker, and left the key in the database; the old probe could not tell, because it read `_source` too. **So this probe plants and reads through the raw socket**: `SocketInterface.dispatch("modifyDocument", {action:"update"})` writes what the server will actually store — including a `-=role` deletion, for a document that genuinely predates the field — and `{action:"get"}` reads the raw server record back. Both plants are witnessed before the reload, so no assertion can pass on a document that was never broken. Then it RELOADS, so the ready-hook path runs exactly as it does for a user opening their world after an update, and asserts: the planted "hireling" is `npc` **in the database**, still for hire at its old rate; a pre-roles inanimate+cart lands `transport`, not the initial `npc`; the retired key is left where it lies, now inert beside a stored role; `forHire` is KEPT, not deleted as it used to be, since it is a schema field again. Finally the Warden's own choices must SURVIVE a second reload — re-role to Monster, untick For Hire — because a blind re-stamp is one step from the trap the retired forHire migration fell into (untick, reload, it is back). The marker is `roles-restamped`, a NEW key: `roles-migrated` is already `true` in every world that has opened, and those are exactly the worlds holding a stored "hireling" |
| `npm run dev:token-defaults` | who arrives **linked**, and with which ring. Three answers since the hireling-role collapse (2026-08-01): a **character** FRIENDLY, an **npc person** NEUTRAL, a **monster** Foundry's own HOSTILE-and-unlinked. Linked because HP edited on a token must reach the sheet; NEUTRAL because role npc is now every person who is not a monster — an innkeeper, a rival — and it was FRIENDLY only while the branch could catch nothing but an already-hired hireling. **A role-less npc is now a PERSON**, the one deliberate reversal: the branch used to exclude it because "a hand-made npc is as often a monster", and the danger was real — widening to plain `npc` would turn all 205 shipped monsters friendly. It is safe because every one of the 220 npc-typed pack documents states its role outright, checked, as does every programmatic creation in `module/`; so the monster case states `role: "monster"` and a document that says nothing came from Create Actor and is somebody. Also asserts a creation stating the RETIRED `role: "hireling"` converts on the way in (npc + forHire) rather than failing the shrunk enum. The defaults live in **`_preCreate`**, not a `static create` override, so every creation route hits them — one case deliberately creates through the **global `Actor`** to stand in for compendium importAll and Adventure import, the routes that never call a static by name and used to skip the defaults silently. A fourth default rides beside them since 2026-08-02: **`sight.enabled`** on every person, PC and NPC alike. `vision: true` was what the generator and the Kettlewright import wrote and it never once did anything -- not a field of PrototypeToken in v14, so `cleanData` pruned it in silence, and `sight.enabled` initials from a `range` of 0 -- so every PC this system ever generated arrived BLIND and its player saw an empty screen on any scene with Token Vision on. Also asserts an explicitly-supplied disposition still wins (which is how it caught `mergeObject` being passed a **non-existent option name**), that a deliberately blind person stays blind (sight is filled, never forced), that promoting a monster to npc re-applies the defaults -- sight included -- while a deliberately-chosen FRIENDLY survives it, and ends on an in-page control: `_preCreate` swapped to the base class's must make the defaults vanish, or none of the green above can fail |
| `npm run dev:doc-lifecycle` | the two ways a batch operation can lie to the rest of the world. **(1)** Regeneration (character *and* NPC) must fire one `createItem` hook per item — the old `actor.update({items})` route creates the embedded documents server-side and fires none, so modules saw inventories change with no item ever created. The probe **runs the abandoned route** as its mechanism control and asserts it really is hook-silent, so the counting assertions are proven able to read 0. **(2)** Deleting a keeper must unlink and stamp every actor connected to it **before the delete resolves** — `_onDeleteOperation` is awaited by the workflow, a per-document `_onDelete` is not, so the same walk in the wrong hook leaves a caller that awaits the delete reading a link to a corpse. The negative control reconstructs that shape in-page (prototype shim + operation hook swapped to base) and must find the child still linked at the instant the delete resolves, then clear once the in-flight write lands. (Until 2026-07-31 this leg tested the same guarantee about a different write — pruning the deleted uuid out of the keeper's `system.containers` array, where the unawaited shape lost a read-modify-write race with itself. That array went with the `container` type.) Its sweep also takes a litter actor's **kept containers** with it — a rolled background can grant a plain-named "Mule", and the probe's own first run leaked a renamed character by sweeping on names alone |
| `npm run dev:listeners` | that re-rendering a sheet does not **accumulate listeners** — the gate review #2 said was missing. Wraps `addEventListener`/`removeEventListener` in-page and asserts the LIVE count (window/document plus still-connected nodes in the sheet frame) stays flat across re-renders of the actor sheet and the background authoring sheet. Listeners on re-render-replaced content drop out of the count on their own, which is correct: only listeners piling up on *surviving* targets are a leak, and that is exactly the shipped bug shape (the click-away save bound per-render in `_onRender`, fixed d46b9c3). Control: `_onRender` shadowed on the instance to add one anonymous frame listener per render — the count must grow by exactly the number of renders, or the counter cannot see the bug it exists to catch |
| `npm run dev:bg-drop-guard` | that **only a player character accepts a background**. Dropping one is not an inventory add — it *changes* the background, deleting everything the old one granted — so on any other type it must be refused outright, with a warning rather than silently. Covers all four routes a background can arrive by (compendium, world item, another character's inventory, and an unlinked token's delta) across npc, hireling and a role-container npc, asserts a refused transfer takes nothing from the donor, and ends on a positive control: a character must still accept one, or every refusal above is satisfied by a handler that refuses everything |
| `npm run dev:bg-drop-order` | *when* the background-swap question is asked, which `dev:bg-drop-guard` cannot see because both defects were correct about *what* the drop does. **(1)** A player in a containers-on world was shown the destructive confirmation before `changeBackground` reached `canRegenerateContainers` — a guard that refuses for **every** non-GM whenever the tab is on — so they answered yes to a question the code could never honour. Needs a real player session (`joinAs(page, "Alice")`, so run `npm run dev:players` first): a GM passes the guard and can never reproduce it. **(2)** The background intercept sat above the same-actor branch, so dragging a *pocketed* background to reorder it raised that prompt and, answered yes, renamed the character's background to the dragged row and deleted the real one's gear. Both legs run twice, the second with the fix neutralised in page — `_mayChangeBackground` swapped for a constant `true`, and `actor` shadowed on the dropped item so the new same-actor term reads false — so neither control keeps a second copy of the code to drift. Restores both settings and every actor it made **from node**, in a finally |
| `npm run dev:i18n-render` | that the localized surfaces come out **localized**, which neither offline check can see: a string routed through `game.i18n` still renders English if the value fed into it is a raw stored token, or if a guard compares a stored English name against a translated one. It swaps `game.i18n.translations` for a copy carrying **unique sentinels** and asserts the sentinel reaches the DOM — so every assertion is its own negative control, since the pre-fix code emitted the English literal and no sentinel matches it. Covers the ability-save label, the Critical Damage banner, the spellbook display prefix (both directions, **and both the em-dash and parenthesised shapes** — the guard knew four stored English forms but only the em-dash localized one, so a translated `Spellbook (X)` got a second prefix bolted on), the equipment-limit tooltip, the delete confirmation, the archetype dropdown, the shop chips, and the **edition label** on the character sheet's source line and the background sheet, which must come from the same key on both. Since 2026-08-02 it also pins **plural forms**: a second sentinel for the singular, read through `formatCount`, because the count is chosen by `Intl.PluralRules` at runtime and no shop chip happens to stock a single-use item — "{n} uses" rendered "1 uses" on 55 shipped items. Since 2026-08-09 it also renders the **gold row's slot tag** (review #13 #12): items-list.html hardcoded `localize 'CAIRN.NSlot_one' n=1`, which only works in a language shipping the `_one` form — a base-key-only translation rendered the literal key text in the tag, and a language whose plural rules map 1 to "other" (ja, zh) was asked for a key its translator was never told exists. The tag is now derived through `formatCount` beside `coinRowLabel` (actor.js), and the leg simulates the base-key-only language by deleting the `_one` form from the shadow and sentinelling the base key, then giving the planted actor exactly one filled coin row (2×coinsPerSlot gold — the first N stay petty) and reading the RENDERED tag. Witnessed: reverting the template line alone reds this leg with `got "CAIRN.NSlot_one"` — the literal key text — while every other leg stays green. **The spellbook plant runs under a settings-read shadow forcing GLOG OFF** (2026-08-10): in a GLOG world the create seam turns the bare "Magic Missile" book into a scroll and the prefix leg reads the (correct) scroll prefix instead of the spell one it is testing |
| `npm run dev:npc-wording` | that the NPC wording **never discards a translation that exists**. `_wording()` prefers a `…Npc` variant on a non-player sheet, and resolved it with `game.i18n.has(npcKey)` — whose `fallback` parameter defaults to **true**, so it consults the English strings too. Every variant exists in `en.json`, so the test was unconditionally true in every language and the NPC sheet served English over a base key the translator had already done. An English world cannot reproduce it (`translations` holds every key), so the probe **builds** the situation: variants deleted from `translations`, sentinels on the base keys, and the variants planted in `_fallback` under a *different* sentinel — so each assertion names the path that produced it rather than merely checking "not English". Ends on a positive control: a language that *does* have the variant must still get the NPC wording |
| `npm run dev:table-results` | that **no deprecated `TableResult` member survives**, and that a row pointing at a WORLD document really resolves. Five members this system used are shims removed in **v15** — `#text`, `#documentId`, `#documentCollection`, `getChatText()` and `CONST.TABLE_RESULT_TYPES.COMPENDIUM` — and all five still *work* on the 14.365 target, so nothing was going to catch this until it broke. So the probe sets `CONFIG.compatibility.mode = FAILURE`, which makes `logCompatibilityWarning` **throw** (and throw unconditionally, ignoring the `once: true` these getters pass), scopes it to TableResult with `includePatterns`, then exercises every path that reads a table: the shop, the eight 2e trait draws, bonds, name rolls, Barebones generation, and the sheet's trait/scar pick-lists. A surviving read throws and the message names the member — so this cannot rot the way a grep would the next time someone writes `r.text`. Then it builds a table mixing a world row, a compendium row and a text row, and asserts all three land correctly. **Both halves carry their own control**, in-page: a deliberately deprecated read must throw under that config (or every green line in phase 1 is vacuous), and the OLD resolution algorithm must still drop the world row on the same table (or the bug was never reproduced). **Phase 4** is the cost of inviting those world rows: resolving by uuid resolves to whatever the row points AT, so a JournalEntry dragged into a Market table (a price list, a shop note) has no `system` and threw inside `getMarketplaceCatalog`, which nothing awaits — the shop silently stopped opening for every player, every category. It unlocks the marketplace pack (which SHIPS locked, so this is only reachable once a Warden unlocks it to customise the shop — the same gesture that lets them drag a row in), plants the row, requires the catalogue to survive, and restores the row and the lock state both in-page and from Node. Its mechanism control calls what `ownedPayload` calls on that row, so a green result cannot mean the row was harmless all along |
| `npm run dev:relic-tab` | that removing a **document-dependent tab** does not leave the sheet standing on it. Recharge is keyed on `item.system.relic`, so unlike every type-keyed tab it can vanish under a sheet that is showing it. `_prepareTabs` sets the active group with `??=` only, so `tabGroups.primary` kept pointing at `"recharge"`, no panel got `.active`, and core's `.tab[data-tab]:not(.active){display:none}` hid the **entire sheet body** — rendering fine, persisting fine, logging nothing. The probe drives the real checkbox so `submitOnChange` commits it the way a user does, on both templates that carry a relic block (`item` and `armor`), and asserts exactly one visible panel plus a group pointing at a tab that exists. **Negative control in-page**: `_getTabsConfig` is wrapped on the prototype to put `tabGroups` back after the guard runs — pre-fix behaviour and nothing else — and the same sequence must then blank the sheet |
| `npm run dev:spellscroll` | **spellscrolls, which are a FLAG on a spellbook and not a type of their own** — so almost everything here is about data a caller supplies, which no render check can see. Ticking Scroll pins petty + one use, drops `equipped`, swaps the art and makes the item un-equippable; unticking restores the book. The pin then has to survive a hostile write (weightless false, five uses) **while a SPENT scroll stays spent** — `uses.value` is the one field the pin must never re-pin, and the two are asserted together because a pin written to hold everything holds that too, silently un-spending a used scroll. The Create-Item leg is the awkward one: "Spellscroll" and "Spellbook" share `value="spellbook"` and only a dataset marker separates them, so it runs under a **sentinel label** — the option is read and the pre-filled name is stored, and only a non-English client can tell those two apart. The ready migration is **planted, not observed**: the old shape (`type: "item"` named "Spellscroll — X") is put in all three places it can hide — a world item, an owned item, and an unlinked scene token's delta — because on an already-converted world every other check here passes trivially. **On this list only since 2026-08-07.** It had an npm script from the day it was written, so `check:probes` was green the whole time, and its one mention in this file was as an illustration in "Reading the results" — which is to say it had never been part of a release run, in the file whose entire purpose is that no probe can be in that position. `check:probes` now asserts this list too Since 2026-08-05 it also proves `glog` **composes** with the scroll flag in both directions, because SCROLL_PINNED/BOOK_PINNED are mergeObjects over `changed.system` and are exactly where a future edit would silently clobber a wording flag. **Every plain-BOOK plant runs under a settings-read shadow forcing GLOG OFF** (2026-08-10): the dev world plays in GLOG mode, whose create seam converts a plain spellbook to a weightless scroll — three legs (the flag section, the display-prefix plant, the dialog's plain-Spellbook option) reddened on the world's own setting until they established the 2e precondition in-page, never by a world write |
| `npm run dev:art-picker` | **the one art picker**, and which galleries each sheet is offered. There were three near-copies of this dialog — the portrait gallery, the container gallery, and core's bare FilePicker on item sheets — so adding the **Game-Icons** gallery meant unifying them first (`module/art-picker.js`), and this covers both halves. **The gallery matrix**, which a refactor breaks silently because a wrong tab set still renders, still picks and still saves: PC = Aspeheim + Custom + Tlomdev, NPC/Hireling = all five, **Monster = Custom + Game-Icons + Tlomdev + Lydia** (Aspeheim is human faces; tlomdev draws creatures too), container/mount = Types + Custom + Game-Icons + Tlomdev, item/background = Custom + Game-Icons with **no Tlomdev — actor sheets only**. **The two withholdings are each other's control**: Aspeheim is kept off a Monster because it is faces, Lydia's off a PC because it is beasts, and both are asserted in one run — an absence leg passes happily when the pane never rendered, but it cannot then also satisfy the presence leg beside it. **The start tab is the trap underneath it**: the picker opens on whichever tab holds the current image, and the old code defaulted to `"shipped"` BY NAME — a Monster has no shipped tab, so that default opens the dialog on a pane that is not there, every tab inactive and the body empty, with no error anywhere. The fallback is "the first pane that exists" and a Monster must land on a real one. **The two-step browse**: the manifest's count of category folders in (a literal here said "23" long after Birds made it 24), thumbnails out, back again, **every tile's caption must be a localized label rather than a raw `CAIRN.` key** — that caption is interpolated from the category key, so a missing one renders as itself, and the folder-count leg beside it stays green throughout (proven by removing `CAIRN.GameIconCategory.Fire` when `fire` landed on 2026-08-04); `i18n:source` gates the key offline, and this covers what it cannot, since its `pascal` is a copy of the picker's and two copies can diverge — and **every folder face must decode** — a manifest naming a file that is not on disk renders a blank tile, never an error, so `naturalWidth` is the only witness. **The Tlomdev pane (2026-08-02) browses the same way with the queries scoped to its pane** — two galleries wear the same class names in one dialog now, and a root-wide folder count reads 24 + 16 as 40 — with the Kettlewright Portraits folder asserted complete (those filenames are load-bearing: the KW importer maps stock picks by them, spaces in the path and all) and the CC BY-SA credit under the grid; a missing Tlomdev tab is recorded as a failure rather than thrown, because the throw was silently skipping every section after it during the negative control. **The Lydia Comer pane (2026-08-04) is the odd one out and gets its own section**: a FLAT grid (asserted to carry zero folder tiles — 17 drawings do not need the category-first shape that exists to keep 1,643 `<img>` out of one dialog), every cell decoded, captions asserted to be the artist's de-hyphenated titles rather than raw filenames, and the credit asserted to say **all rights reserved and NOT Creative Commons** — hers is the one grant here that is not a public licence, and a viewer must not read the CC BY-SA line under the neighbouring grid as covering it. **Its real weight is the PAIRING**: two of the five galleries ship a portrait and a separate token, and Lydia's halves carry *different extensions* (`.jpg` square, `.png` circle) because her grant forbids re-encoding — so `token === portrait`, right for the other three galleries, is wrong here and is exactly what the paired lookup emits when it fails (`?? img`), silently, on a sheet that looks correct. **The portrait die's folder rule (2026-08-02)**: it re-rolls within the folder the current portrait came from — a tlomdev beast stays a beast and is its own token, an Aspeheim face stays Aspeheim with the PAIRED token file, a Lydia creature stays a creature with ITS paired token, and only an image from no known gallery folder falls back to the auto-assignment pool, which the leg computes against the world's actual custom folder rather than assuming it empty. That fallback leg also asserts it never lands in Lydia's gallery: the die on an unrecognised image must not turn a hireling into a black pudding. Picking commits on an actor AND on an item, which take different write paths (only the actor pairs a token). Item art goes through the real `editImage` action, not the private method, because the point is that core's FilePicker no longer receives it. Also re-asserts the 2026-08-02 INVERSION: picking a glyph is art only — the cells carry no `data-class` claim and a sack under barrel art stays Kind sack (the pre-fix reading, the pick re-kinding it, is the control's witness). **Negative control in-page**: the Monster exclusion defeated by shadowing `npcRole`, and the Aspeheim tab must come back; without it, "no Aspeheim on a monster" passes just as well when the dialog fails to open. The stash control (sources out, probe kept) reds the matrices, the Tlomdev pane AND both roller legs — the die rolled a beast into an Aspeheim face under the old code |
| `npm run dev:art-paths` | that the **`art/` move (2026-08-04) reached every document surface that stores an image path**. Moving art is never cosmetic: a path is COPIED onto a document at creation and never re-read from the system, so every world made before the move points at where its art used to be, and `migrateArtPaths` rewrites by PREFIX so hand-picked art travels too. **The failure this exists for is a surface the migration forgot**, which is invisible in aggregate — sheets look right, compendiums look right, and one Warden's own roll table serves broken images forever. So it plants an old path on all seven kinds independently: a world Item's `img`, an Actor's `img`, that same Actor's `prototypeToken.texture.src` (a second field on one document), an owned Item (embedded, its own copy), an **unlinked Scene token** (holds its own texture), and a **RollTable result** — a SNAPSHOT rather than a live read, which is the surface the earlier `.png → .svg` migration never needed and this one does. **A seventh surface since 2026-08-04**: a path already under `art/` whose FORMAT is stale. Lydia Comer's gallery shipped as `.jpg` squares and `.png` circles until the artist extended her grant to permit format conversion, and it is WebP now — which the prefix table structurally cannot express, because the front of the string is already right and the EXTENSION is wrong. **It is planted ONCE PER RE-ENCODED GALLERY** (Lydia's, and tlomdev's 298 category drawings the same day): they are separate rules with separate `from` patterns, so one entry proves only itself, and the window between the `art/` restructure and the WebP conversion is the state of every world that has been tracking `dev`. The two rules CHAIN, and the Scene-token leg is what exercises that across *different* extensions: a world last opened before the restructure holds the old prefix *and* the old format, both rules must fire in order, and the migration runs once so there is no second pass. That leg was written expecting the move alone and went red the moment the re-encode rule landed, which is how the chain was confirmed real rather than assumed. **The owned-Item leg is the same lesson arriving a second time**: it named a tlomdev `.png` on both sides, correct while that gallery was only moved, and the tlomdev re-encode rule turned it into a claim that the migration must NOT finish — which is the argument for writing each expectation out by hand rather than deriving it from the tables under test. The readiness poll had to learn the same distinction — it tested for a stale PREFIX, which a re-encode surface never carries, so it reported finished the instant the path was planted; it now names **every** re-encoded gallery, because omitting one fails nothing visibly and instead lets the assertions race the migration, the failure that passes on re-run and gets called a flake. It asserts the planted values actually LANDED before reloading, since Foundry can normalise an `img` on create and a plant that never stuck makes every later leg vacuous. **Three surfaces the review added 2026-08-04, after all five reviewers converged on the gap**: an unlinked token's **ActorDelta** — its `img` and a delta-only Item, both a second copy of the art that migrating the base actor and the canvas texture cannot reach, so the token looked fixed on the map while its sheet portrait 404'd; and an Actor in a **LOCKED world compendium** — a Warden's curated pack is the copy-at-creation case with no second chance, the migration unlocks/writes/re-locks, and the probe asserts the lock came BACK. That lock assertion joined the readiness poll's `done()` the hard way: the re-lock is a settings round-trip that lands after the last document write, so a poll satisfied by paths alone reads the lock mid-window and reports it missing — this probe's own trap, third appearance. **Negative control, both directions**: `icons/` deliberately did not move and an external URL was never ours to touch, so both are planted too and must come back byte-identical — without them "it rewrote everything" is equally satisfied by a migration that rewrites indiscriminately, which would break every class icon in the world it was meant to fix. Polls for the rewrite rather than sleeping: `game.ready` does not mean the migration finished, because `Hooks.callAll` does not await an async callback and this phase runs behind two others that each make server round trips. Every document is created here and deleted from a Node-level `finally`, so a throw cannot leave `zz-art-path-*` litter for the next run to pass on. The omission that proved the point was real — `lydia-comer/` was left out of the prefix table on the reasoning that it had never shipped in a release, and `dev:smoke` found a dev-world token still on it; `dev` mirrors to GitHub in seconds so people can test unreleased code, so "it never shipped" is only ever true of tags |
| `npm run dev:roles` | `system.role`, the one discriminator on the NPC sheet (docs/npc-roles-plan.md) — it replaced the For Hire and Inanimate checkboxes. Picking Transport/Container drops HP, STR/DEX/WIL, Armor, Deprived/Panicked, the Rest / Restore / Die-of-Fate buttons and every derived condition, while KEEPING name, inventory and tabs; only Hireling shows the day rate; the Career and Kind rows ride the role too. **Gold follows the role since Round 2**: hidden on Mount/Transport/Container with the stored value asserted intact through the round trip (hiding a purse is not emptying it), and a Mount keeps its stat block while losing the counter. **The header gap**: the vitals must reach the portrait's foot (one-sided since 2026-08-02 — the Faction row made even the monster's stack taller than the portrait, so "below the foot" is the header growing, not the band returning), directly above STR/Armor — the in-page control now INJECTS a band the measurement must detect, because the old restore-the-margin control lost its slack when the stack outgrew the column. **Edge rules**: a PC is never kept by a PC (refused, nothing written, and `CharacterData` declares no `connectedTo` — the write is unrepresentable, not merely refused), an NPC never keeps a PC, a connected actor cannot be stolen (single-parent enforced in `connectActor` itself, the path the picker filter never covered), PC→NPC connect copies the PC's ownership. **The cap (2026-08-01)**: ten children seeded through creation data, the eleventh `connectActor` refused with nothing written, and the differential witness — one child fewer and the same call lands, so the refusal was the count. **The header connection line (2026-08-02, replacing the child end's tab)**: a connected sack names its keeper on the line ("Connected to: X"), a for-hire PERSON reads "Hired by: X" with the For Hire checkbox ON the line, unticking it flips the label on the same document, a MOUNT storing `forHire: true` still reads "Connected to" (the role gate's load-bearing witness — the flag's schema initial is true), the header's detach unlinks + stamps and the line then reads "Formerly connected to X" from the DOM, and an unconnected one reattaches through the header's Connect picker — which offers **characters only**, the hireling asserted absent from it, and only the PC's end renders Add Connection at all. **And the player leg — INVERTED 2026-08-01 with the player-usable verb**: Alice, established by the probe and owning both ends, now gets the edge controls (Connect + per-row unlink on her PC, detach on her sack) and her direct `connectActor`/`unlinkOwnedContainer` calls LAND end-to-end — link written, sync flag relayed, the live GM client answering with the exact ownership shape, her break costing her OWNER (default LIMITED, entry stripped — the designed outcome). The re-proven fail-witness is the one-end-foreign differential: a sack she does not own refuses her, which only the both-ends term can be refusing (confirm stubbed so an unfixed unlink fails instead of hanging) — and since 2026-08-02 the same differential ON THE CONTROL: a sack she owns under a keeper she does not renders its header label for her while withholding the break control. **The derived conditions**: `_computeStatContext` reads `dead = STR <= 0` — exactly the value a crate sits at — so without the isThing guard the sheet announces that a barrel is Dead, Paralyzed and Delirious at once (banners render from a separate context array in a section that stays visible). **Tab ORDER**: the npc sheet runs Description → Items → Notes (THREE tabs since 2026-08-02 — the Connections tab is the header line now, asserted absent across person/thing/mount/monster) in both the nav and the panels, and a FRESH sheet *opens* on Description — which takes the `tabGroups` field override, because core seeds the group from static `TABS` at construction and `_getTabsConfig`'s initial never gets a say on first render; the in-page control forces the pre-fix "items" seed back and the sheet must reopen on Items, or opens-on-Description is not load-bearing. The character sheet is asserted still Items-first — that leg is what stops a "reorder both" regression, and it doubles as the order-reader's differential witness. **And the select must not be one-way**: it sits OUTSIDE every block it hides. Drives the real select so `submitOnChange` commits the way a user does; **negative control in-page**, the guard defeated by an instance shadow over the `isThing` getter, and a zero-STR thing must then show the Dead banner again. Owns the **FLAT graph (2026-08-01)**: keeping is a TYPE privilege — an npc person cannot keep her own backpack any more (the fail-witness: nothing but the type wall refuses that pair, and the same sack then connects to the PC), a mount cannot keep, a monster cannot connect, and a legacy npc→npc chain seeded through creation data cannot be extended (`wouldCreateConnectionCycle` survives behind the type wall as belt-and-braces). Owns the **PC count (bug 1's residual, 2026-08-02)**: the parenthesised number on the character's Connections tab gets its first direct witness — (0) fresh, (1) when a mount connects under the OPEN sheet, (0) when it goes; said plainly, these legs pass against pre-fix code too, because the count was never wrong — the missing thing was the witness, and their failure mode (the sheet-sync no-opped) is controlled in `dev:container-unlink`. The old vanishing-tab reset leg died with its scenario: the npc tab set is static now. **And the unlinked-token leg**: a synthetic token actor (`actorLink: false`) is not in `game.actors`, so it can never appear in a keeper's list nor resolve as one — asserted as two world/synthetic pairs now that keeping is character-only: an npc's token copy cannot BE connected while its world actor can, a character's token copy cannot KEEP while its world actor can, the copy's header offers no Connect control while the world actor's does (the tab is gone for everyone, so the header control is the sheet-level witness now), and a direct `connectActor` from the synthetic character is refused while the world character's same call lands. That is the defect a Backpack hit in the dev world: connected to the world "Bat, Vampire" while the sheet on screen was the token's, same name and art, tab reading 0 forever. Also owns the container **art gallery**: one cell per GLYPH (13 cells for 14 classes since 2026-08-02 — mule owns the donkey head, and the coffin glyph left with the retired funeralwagon class; the asset still ships for the Burial Wagon doc), **none carrying a class claim** — a pick is ART ONLY by ruling, asserted as art+token changed with Kind and capacity untouched; the defaults arrive through the KIND instead (picking barrel stamps the unset capacity), a typed capacity survives re-art, Browse keeps the stored Kind, picking a KNOWN Kind brings its defaults, and a Warden's own word stands the Type select on Other…, verbatim in the revealed input, bringing none. The gallery poll replaced a fixed 900ms sleep (pickArt awaits three manifests before its dialog exists — a captured one-off red). Owns the **Faction row (2026-08-02)**: present with its die on role npc AND monster (its own `showFaction` gate — it no longer rides Career, which is why Monsters never saw it), absent on mount/container, surviving the round trip; and the die is **WORLD-FIRST** — a world table named `Warden: NPC - Faction` carrying a sentinel row wins over the shipped copy through the real anchor click, and the differential deletes the world copy so the same click must answer from the shipped stock 20, membership-checked against the pinned SRD Type column; sentinel tables and actors swept from Node, on the way in and out. **Parked Connections UI (2026-08-09)**: the header-line and keeper-picker legs run under the in-page `connections-ui-enabled` read-shadow; the For Hire leg runs WITHOUT it — that differential is what proves the day-rate control survived the parking — and the character tab-order leg expects no Connections tab in the parked default |
| `npm run dev:container-unlink` | trash vs unlink on the keeper's Connections tab — two intentions, two icons, and only one of them destroys anything. The tab used to carry a single trash icon that **did not delete**: it asked "Delete X?", then filtered the owner's array and cleared `keeper`, leaving the actor alive and belonging to nobody. Harmless-looking while a container was a bag of slots; under the container rule (an actor connected to nobody IS a loot pile) it silently drops a pile in the world every time a Warden tries to destroy a crate. Asserts trash really deletes, unlink leaves it alive with `connectedTo` cleared and off the owner's tab, and that the previous owner's **name** is snapshotted as a string — then deletes the owner and requires the line to survive, which is the case a uuid could not have survived and the whole reason the field is not one. The "Formerly connected to" label is read off the RENDERED header connection line since 2026-08-02 (the derived `system.ownedBy` died with the child end's tab), which is also what a Warden actually reads. `DialogV2.confirm` is stubbed rather than answered by polling: a settled DialogV2 outlives its promise in the DOM, so a poll finds the dead one, clicks it for nothing, and the next confirm hangs forever. **Negative control in-page**: `deleteOwnedContainer` is swapped on the prototype for the old implementation, and the actor must then survive its own deletion. **Runs entirely under the parked-UI read-shadow (2026-08-09)** — its DOM witnesses (the Formerly line, the tab rows) render only with the Connections UI restored; the document methods it is about never consult the flag |
| `npm run dev:socket-grant` | the player→Warden Actor-creation broker, **as a real player against a live GM client** — the positive path is the standing gate for `"socket": true` in `system.json` (without it the server relays nothing, with no error anywhere; the server reads the manifest at **startup**, so this needs a restart after that line changes). Then two attacks: a payload naming a GM as the requester must be ignored in favour of the server-authenticated sender id, and an embedded-Item uuid (which resolves, and delegates ownership to the parent) must be refused as a connection target. **Negative control in-page**: the pre-fix handler is re-registered beside the fixed one and both attacks replayed — the old code must mint both actors, or the attacks prove nothing. **And the cap (2026-08-01)**: the broker is the WALL — the player-side clamp in `grantContainers` runs in the player's browser and cannot bind — so raw emits past it at a PC seeded to `maxConnections()` must mint nothing, with the GM console naming the clamp; the witness is one child fewer and the same two-payload emit minting exactly one (the slice is the headroom, not all-or-nothing). The broker also writes the CONNECTED ownership shape exactly — default OBSERVER + the requester's OWNER, nothing else non-GM — replacing the old wholesale copy |
| `npm run dev:connections` | the player-usable connection verb and its ownership automation, end to end as a real player (see the probe header for the full leg list): connect with both ends owned → link + sync flag in one write, the live GM client answering with the EXACT connected shape and clearing the flag; the one-end-foreign differential refused with nothing written (the both-ends wall's fail-witness); the break landing and COSTING her OWNER, with the positive control that her next write really fails; the cap refusing her player-side; the GM-ready SWEEP processing a flag set while no GM client existed (the GM context is closed and reopened mid-probe — the socket emit died unheard, the flag did not); hostile emits changing nothing (a flagless uuid's default-0 ownership is the observable the broken shape would have raised; embedded-Item and compendium uuids refused as targets); and `_preCreate`'s LIMITED default — bare npc LIMITED, monster NONE, explicit data wins, the global-Actor path included, with the prototype-swap control making the default vanish. Needs a live world; establishes Alice itself. Since 2026-08-02 it also refuses a **crafted** connect: the both-ends rule lives entirely in the client and `system.*` has no server wall, so a player can point their own npc at a stranger's PC, raise the flag they are allowed to set, and ask the Warden's client to sign it. The refusal keys on the authenticated `senderId` and must CLEAR the flag, or the check is walked around by waiting for the next GM reload (the ready sweep re-runs every flagged actor with no requester). Asserted by CALLING `syncPendingOwnership` with a requester rather than by emitting — an emit is answered by whichever client holds `activeGM`, which in a world a human has open can be a stale tab — with the same requester on her OWN PC as the positive control. **Also owns the PARKED default (2026-08-09)**: tab and panel absent on a rendered PC sheet, drag-to-connect refusing null, the in-page read-shadow restoring the whole UI, a stale sheet's control refused by the handler wall, and the vanished-tab reset landing a re-parked sheet on Items |
| `npm run dev:connections-migration` | `flattenConnections` (marker `connections-migrated`), every branch seeded by the probe and run through a real reload: a PC-rooted chain RE-POINTED (the hireling's sack now belongs to her character), an already-flat link kept, an npc-rooted chain unlinked + stamped, a dangling uuid cleared with the STORED `formerlyBelongedTo` kept, a raw-seeded cycle broken at both ends; connected non-monsters wearing the EXACT shape of their FINAL keeper with junk entries REPLACED away (ForcedReplacement, not a merge); unconnected NONE npcs raised to LIMITED — beside the two witnesses that make it believable: a NONE **monster** seeded identically and untouched (the exclusion's fail-witness) and a Warden-raised OBSERVER default never lowered. Ends on the marker holding: a second reload moves no seeded document's `modifiedTime`. Leaves the marker TRUE — a probe that left it false would re-run the migration over the user's world on the next GM load |
| `npm run dev:compendium` | the shared name→document lookup in `module/compendium.js`. **Counts** full pack loads while opening the shop, because the catalog is correct either way and the entire defect is how much work happens — a functional assertion cannot fail. Also that a missing table degrades to `undefined`/`""` instead of throwing, and that adding a *row* to a shipped table invalidates the sheet's pack cache |
| `npm run dev:icon-canvas` | the icon migration reaches scene tokens and the canvas ends correct after a reload |
| `npm run dev:enc-damage` | the damage flow, including a real canvas draw — encumbered/panicked HP must survive both damage and a sheet submit (asserted on SOURCE values, since the derived ones read 0 either way), and the chat card's **Apply-damage button**: a card carrying two `;`-joined token ids is clicked **on the icon inside the anchor** (where a real pointer lands, and what keeps `event.currentTarget` distinct from `event.target`) and both tokens must lose HP. Control: the same card with its handler unwired must change nothing. Added when the button lost the repo's last jQuery call — it was the one handler no probe exercised. Since 2026-08-01 it also owns the **role re-key** of the encumbrance rule (`livesByPlayerRules`): a loaded **mount** keeps its HP and its input still submits; a role-npc **person** at capacity reads 0 with the stored value intact and the submit stripped, exactly a PC; and a full **container** neither reads 0 nor loses HP edits — the leg that proves the re-key did not simply widen review #5's un-editable-HP bug (a thing's sheet hides the HP input, so its guard is interrogated with a synthetic payload). Each leg's fail-witness is an instance shadow of the getter, run in **both directions**, which also proves the derived zero and the submit strip read the ONE getter rather than two conditions that can drift. Since 2026-08-02 it also pins the button to the scene the roll was made on: the party **views a second scene**, the log is forced to re-render (rebinding is where the old failure became permanent) and the click must still land — plus a mixed batch of one live and one dead token id, where the survivor takes damage and the miss raises a **notification** instead of being swallowed. Since 2026-08-05 it also owns the **Fatigue-is-never-refused** rule: a character built to EXACTLY the limit (never over it, or "used exceeds max" would be true before the click and prove nothing) has the real `data-action="addFatigue"` button **clicked twice**, and both must land, taking slots past the maximum — clicked rather than called because the old code refused in TWO places, its own guard and `createOwnedItem`'s behind it, so removing either alone left the button refusing while every unit-level assertion passed. Control: the old refusal shadowed back onto the instance blocks the same click. The same section pins the **boundary**, which is the part most likely to erode — an unflagged `createOwnedItem` on that same full character must still be refused, because overflow is owed to Fatigue and to what generation grants, never to ordinary acquisition, and a direct three-item grant must land whole. **2026-08-06 — the chat card the Warden actually reads.** The **Scar card must name whoever was scarred**: `RollTable#draw` forwards only `messageOptions` to `toMessage` and never `messageData` (roll-table.mjs:139), so the card fell through to `toMessage`'s default `ChatMessage.getSpeaker()` with no argument — the VIEWER'S own assigned character (roll-table.mjs:57) — and a monster scarred by a player's hit was posted under the PLAYER's name over core's "Draws a result from the Scars table", reading as though the attacker had drawn a scar for herself. Asserted on both `speaker.token` and `speaker.actor`, **positively**: "`!==` the attacker" was tried and DOES NOT DISCRIMINATE — with the fix reverted, core's getSpeaker resolved to another controlled token, so the negative form stayed green while the card was still wrong. In-page control: a bare `draw()` alongside it must still misattribute. Plus the scar banner (present, centred, bold, glowed) and that chat body text computes **above core's 14px** — an inequality, not the literal, since the ask was "up a point". Last, **Apply damage is Warden-only, checked as Alice** because a GM cannot see this by looking: her copy must contain no `.apply-dmg`, while the STORED card still carries it — that second half is what stops the first from being a tautology, and it reads `flavor`, not `content`, because both real producers ship the card as the roll's flavor. **2026-08-07 — three more card rules.** The scar card carries **zero `.dice-roll`** while its scar text survives (the roll handed to the draw is a CONSTANT, so rendering it printed the damage as formula AND total, under a damage card that had just given it) — with the pre-fix call, which DOES forward the roll, as the in-page control, because without it the leg would pass just as well on a table with `displayRoll` off. The **FLAVOR** line computes above core's 14px, measured on the scar card's own flavor: flavor is not inside `.message-content` but a `<span class="flavor-text">` in the message HEADER (chat-message.hbs:24), so the earlier `.message-content` bump missed every flavor line in the log — witnessed at **12px**, two points UNDER core's body. The Apply control **draws a real glyph**, read from `::before` rather than from the class name, since a Font Awesome class absent from the shipped font renders an empty box with no error and no missing element. **Apply marks the card and applies once** — seven legs, all asserted on ACTOR HP rather than on the summary text, because the text is the affordance and the HP is the hazard: the first click lands, the message is FLAGGED (a DOM-only disable would be undone by the next chat re-render), the card gains the summary, the control reads as spent, and a SECOND application changes nothing. The second click is made by calling the handler **directly**, not by `.click()` — `pointer-events: none` means a real click never lands, and "the CSS stopped it" is exactly the reassurance this leg must not accept; its control is the same call with the pre-fix signature (no message), which must still apply, or the refusal leg would pass because the path is broken. Witnessed in both directions separately, which is the point of having two layers: disabling the renderer reds the summary and greying legs while the second-application leg stays green, and removing the guard does the exact opposite. Last, the **attack line**, driven as **Alice** because a GM owns everything and cannot see the leak by looking: a card rolled at a visible and a hidden token names the visible one and NOT the hidden one, while the WARDEN's copy of the same card is read and must carry both — that second reading is what stops "Alice is missing the name" from passing when the name was never resolved at all. With the gate removed her card read "…attacks the ZZ Seen Foe and ZZ Unseen Foe!", a real leak. With only a hidden target she gets the original weapon sentence, never a half-written one. The sentence carries the **weapon** and **no article** — an early cut said "attacks the {target}" and read wrongly for any named NPC ("Lisbeth attacks the Thaddeus!"); both are their own legs, and dropping the weapon leaves exactly one red while the no-weapon fallback key still renders a clean sentence. **The probe's fixture is created per run** — the dev world has no actors. **2026-08-07 — round 2 of the same card.** The Apply control also reads in the **failure colour**, asserted as the two COMPUTED colours being equal **to each other** rather than against a literal `rgb(206,7,7)`: the ask was "the same as failures", so a literal would go red on core retuning its own token — not a regression — and would stay green if the failure rule drifted off the token while this one did not. The comparison element is planted, read and removed; a computed style is READ and nothing is written. Witnessed by deleting the declaration, which reds that one leg while the glyph and size legs stay green, and names the colour the control had before: `rgb(68,68,68)`. And the **target's name is bold**, which introduces markup into a label that deliberately had none: asserted on the `<strong>`'s OWN text (a `<strong>` wrapped round the whole sentence would not satisfy it), on its computed weight, on Alice's copy bolding only the name she is told — so the bolding runs through the same visibility gate as the sentence — and by an **injection leg**, a token named `<img src=x onerror=…>` which must leave the label holding exactly one element, ours, with the payload's flag unset and the name still reading literally (escaping it away would hide the attack from the Warden who has to notice it). **That leg needs TWO witnesses and the difference is the point**: reverting to `textContent` reds the four bold legs but leaves "payload fired=false" GREEN, because the pre-fix code was safe too and the witness proves only that the bolding shipped. Only the naive `innerHTML` fix — the one a future edit reaches for — reds the security half, with `["strong","img"]`, the payload FIRED, and the name mangled to "ZZ  Foe", while both bold legs stayed green. A naive fix looks exactly like a landed feature. **Since 2026-08-07 it owns the UNTARGETED card's splat and the armor breakdown.** A roll made with nothing targeted used to carry no Apply control at all, so the number sat in the log and had to be applied by hand. The anchor is built at RENDER rather than in the template, and the leg that tells those two designs apart is a PAIR: the STORED card must have no anchor (which is exactly what a card rolled before this change looks like) while the DOM has one — a template-only fix makes both true and can never reach the log's existing cards. A **legacy-shaped** card (the wrapper with a plain child div, before `.dmg-label` existed) gains one too. Clicking opens a picker: two groups with **monsters first**, PCs listed rather than filtered out (falling damage and friendly fire are real), Cancel applying nothing, and two ticked creatures each taking the FULL roll. **The picker proposes NOBODY** (user ruling 2026-08-07, superseding a pre-tick from the canvas selection): in play that offered the ATTACKER as her own victim, because the gesture before a damage roll — opening a combatant's sheet — SELECTS the roller, so the heuristic was biased toward exactly the wrong answer. A targets-only variant was offered and rejected too, which is why the leg **establishes BOTH signals and proves both ignored**: a token is selected AND another targeted, and the leg asserts `controlled > 0 && targeted > 0 && ticked === 0`. It states its own precondition on purpose — with nothing selected, "nothing is ticked" is true for the wrong reason, and the witness that removes `control()` must red it rather than green it. A **spent card does not even ask**, and that leg states its own precondition (`flagged &&`), or it goes green on a card that never got spent. Both paths through `targets.split(';')` are asserted not to throw, because an untargeted anchor is the only way to reach the handler with no `data-targets` at all — the witness reproduced `TypeError: Cannot read properties of undefined` on each. Two probe defects were fixed by those witnesses and both are the same shape: an unguarded `anchor.click()` and an un-awaited handler rejection each killed the run with a HARNESS error rather than reddening a leg, which reads as load or a flake. The **armor breakdown** is asserted at three points — armour named (`4 (6 damage − 2 armor)`), armour 0 dropping the bracket ENTIRELY (a ruling, not an oversight), and an absorbed hit KEEPING it, which is why the drop rule is armor-based and not result-based. The absorbed fixture uses armor **3, not 5**: armor is hard-capped at 3, so a 5 arrives as a 3 and the leg fails on its own expectation. And the HP/STR lines are compared against the **pre-fix concatenation posted through the same path**, never against a literal — chat content is parsed and re-serialized, so the bare `>` in `=>` returns as `&gt;` and a literal expectation fails while the card is identical. Witnesses, each reddening exactly one leg: always showing the bracket reds only the armour-0 leg; smartening `=>` to `→` reds only the unchanged-card leg. **2026-08-07 — the scar banner's COLOUR, and three status cards.** The scar glow is asserted against a planted `.dice-total.failure` swatch and compared **to each other**, never against `rgb(206,7,7)`: it was the last consequential-damage surface in the log still reading teal, and the pre-existing "centred, bold and glowed" leg is structurally BLIND to colour (`textShadow !== "none"`), which is why it stayed green through the whole teal era and stays green under this witness. The banner text must also end in `!`, since `i18n:check` cannot see a changed English value. **Critical Damage, Stabilized and Dead each post the SHEET's own status bar to chat** — the same `.status-banner` markup, so critical and dead needed no new colour and no new string. Legs: marking posts one bar, clearing posts `stabilized`, STR reaching 0 posts `dead` (by a SHEET edit — `dead` is DERIVED as `STR <= 0`, so there is no flag to watch and the pre-state is stashed in `_preUpdate`), `abNoStatusCard` silences it, and the bar names nobody in its BODY (the header names the actor, so no authored text reaches the markup). Three of these legs exist because nothing else can see what they check. **The one-card leg needs ALICE**: `_onUpdate` runs on every connected client, so a missing `userId === game.user.id` guard posts one card per logged-in user, and the witness returned `["critical","critical"]` — invisible with a single browser. **The ORDER leg** asserts `["damage","deadbar"]` on message positions, not existence: `applyToTarget` awaits its `update()` and only then calls `_showDetails`, so a hook-posted death card races ahead of the card that explains it — the witness returned `["deadbar","damage","deadbar"]`, both out of order and duplicated. **The corpse leg** is the only one that exercises the transition guard at all: the "a no-op posts nothing" leg passes because FOUNDRY drops an unchanged field from the diff, so `_preUpdate` never stashes — removing our own check leaves it green, which the witness proved. What the check does protect is that clearing `critical` on a dead actor must not claim a stabilization, and that leg reds under the same witness. A **corpse still gets feedback**: hitting an already-dead creature is not a transition, so `_showDetails` posts the bar directly, or a click on a corpse would show nothing at all. **The one-card leg has a standing confound and now reports it**: the guard is per USER, not per SESSION, so a Warden with a second tab open on the dev world gets two cards and every status leg reds exactly as a missing guard would. It cost a diagnosis on 2026-08-07 and the tell is in the failure message — the leg prints the cards' AUTHORS and the active-user list, and two cards both authored by "Warden" with one user connected is two sessions, not a regression. Settled independently by counting `updateActor` fires on one client (one) against cards posted (two). Close the tab and re-run; do not change the guard. **Since 2026-08-07 the DETAIL card also says where its damage came from.** Both halves are read off the card that was clicked — the attacker from `message.speaker`, the weapon from the `data-weapon` datum the template stamps — so nothing is captured at roll time, and the sentence is built per VIEWER from a flag rather than baked into stored content. Driven through the REAL Apply control on two clients: the flag holds IDS AND A RAW NAME (never a finished sentence), the stored card must NOT contain the attacker's name, the line reads whole, it is the FIRST element of `.message-content` (the ruling was above the numbers, and "somewhere on the card" would not test it), it computes italic, **Alice sees the identical line** — knowing what hit you is not Warden-only information — and a roll with no weapon renders the other key rather than a dangling "'s ". The **injection leg** demands the tag list be EMPTY, not `["strong"]` as on the attack label: this line has nothing bolded, so the whole sentence is ONE text node, the strongest form of the rule. Four witnesses, each reddening only its own: dropping the flag write reds the naming, placement and styling legs together; `innerHTML` reds the injection leg with `["img"]` and the payload FIRED while placement and styling stay green; `append` instead of `prepend` reds only the placement leg. **The flag-write witness also caught a probe defect worth remembering** — written as `alice === gm` the two-client leg was satisfied by two EMPTY strings and stayed green with the feature entirely switched off, so it now asserts the positive as well. **Since 2026-08-09 it also owns `auto-record-scars`** (Warden switch, default OFF, user ruling — the one automated sheet write in the damage flow): three strikes at the exactly-to-zero trigger, all through the real damage flow. Switch on + PC — the drawn scar lands CHECKED on the sheet, asserted against the EXACT table row (the draw's roll is the constant damage, so damage 3 names its row deterministically, in the same English source text `.scar-check` persists), `scarEnabled` comes on with it (or the record sits invisible behind the sheet's opt-in), and the LEDGER stays silent with change-log forced on — the write carries `abNoStatusCard` like every damage-flow write. Switch on + monster — card only, BY RULING and not by schema: the npc model HAS a scars field ("a person is a person"), so only damage.js's type gate keeps that leg green. Switch off (the shipped default) + PC — sheet untouched. PC tokens are LINKED (`_preCreate` sets `prototypeToken.actorLink` for characters), so the probe reads the world actor without the synthetic-delta trap; the ACTOR is polled for the write, not just the card — the write follows the card inside the same un-awaited draw. Witnessed: the write removed reds exactly the two positive legs (`system.scars=[]`) while all three controls stay green. Settings snapshotted and restored in-page. |
| `npm run dev:hazard` | **the Warden's damage** — traps, environments and conditions, which every other damage path could not express because it starts at a weapon on somebody's sheet. Its own file rather than more legs in `dev:enc-damage`, which is already the largest probe here. A hazard is a weapon nobody is holding: the dialog posts the ORDINARY damage card, so the splat, the target picker, the detail cards, the STR save, the Scar draw, the status bars and the attribution line all come free — what is new is the button and the POOL. **The tool is read in TWO places**: `ui.controls.controls.tokens.tools` is what the system registered, the DOM button is what a Warden can click, and a tool registered into a control set that never renders is missing from the second reading only. Its tooltip is asserted to have LOCALIZED, since `title` is an i18n key core resolves at render (scene-controls-tools.hbs:5) and a missing one shows as the literal `CAIRN.…`. **Alice gets neither the tool nor the action, witnessed SEPARATELY**: the tool's absence is the affordance and `openWardenDamage`'s refusal is the enforcement, and `visible: game.user.isGM` is evaluated ONCE when the palette is first prepared (scene-controls.mjs:378-380), so it is not a live gate. Removing the refusal reds the enforcement leg with `refused=false dialog=true` while "she has no tool" stays green. **That witness first HUNG the run rather than reddening**, because with the refusal gone the call opens a DialogV2 nobody answers and its promise never settles — the leg now races every such await, and the rule it costs is that a witness must redden a LEG, including awaits that only open a dialog once a witness is applied. The dialog is driven through the real palette button: the formula field arrives PRE-FILLED (set with `setAttribute`, since an element handed to DialogV2 is serialized and re-parsed and a `.value` property never reaches the markup), the placeholder survives (element content bypasses `cleanHTML`, whose allow-list would strip it), four pools render with HP first and selected, and the card carries `data-pool` and `data-hazard`. **Targets come from `game.user.targets`**, as `#onRollDamage` does — an aiming gesture the Warden made, unlike the canvas SELECTION the target picker was ruled never to read. **The Warden's own words stand**: the attack-line rewrite stands off a hazard card, and the witness that removes that guard produces "Warden attacks ZZ Hazard Victim!" — the Warden cast as the attacker of their own trap. The label is also an injection leg, and its tag list must be EMPTY. **A trap speaks as nobody** (`actor: null, token: null`): a bare `ChatMessage.getSpeaker()` infers a speaker from the CONTROLLED token and then from the user's impersonated actor (chat-message.mjs:243-255), so the trap would have been posted in the name of whatever the Warden had selected. A bad formula is refused rather than defaulted. **Where it lands** is asserted per pool by clicking the real Apply control: STR loses exactly the damage with **ARMOUR NOT SUBTRACTED** (fixture armour 3 against 2 damage — the leg that catches an ability branch reusing `_calculateHpAndStr`, and the witness that applies armour reds it with STR unmoved), HP untouched, and the STR save offered by combat's own `newStr < str` branch. DEX to 0 posts the paralyzed bar and WIL to 0 the delirious bar, **asserted on POSITIONS as `["damage","paralyzed"]`** — the witness that drops `abNoStatusCard` from the ability write returns `["paralyzed","damage","paralyzed"]` for all three abilities, out of order and duplicated, which an existence check would have passed. No Scar on an ability hit, STR to 0 is death, and a corpse is not asked to save. Two legs guard the cards ALREADY in the log: **no `data-pool` means Hit Protection**, and an **unrecognised pool falls back to it too** — the pool is spliced into `system.abilities.<POOL>.value`, so the witness that drops the whitelist leaves the damage writing a field nothing declares and the target takes NOTHING, which from the Warden's chair looks like a control that did not work. Needs `npm run dev:players` (Alice). **Since 2026-08-08 it also owns the DICE BUILDER** — die buttons and a Sum / Keep-highest control that write INTO the formula field, which stays the single source of truth (nothing downstream can tell a built formula from a typed one). Two layers, witnessed separately: `composeDiceFormula`/`parseDiceFormula` as pure functions, with every emitted string fed BACK through `evaluateFormula` and the resulting roll formula checked for `kh` exactly where keep-highest was meant — deterministic, asserting the rewrite and never a die; and the LIVE dialog through the real palette button, where clicks must change the field at all (DialogV2 serializes the built element, so listeners wired in `buildForm` are discarded — the witness freezes every UI leg at “1d6” while the pure legs stay green), a hand-typed `2d6 + 3` greys the buttons and ✕ re-enables them, and a built `2d6` reaches a card through `Roll.validate`. **The dialect legs are the load-bearing pair**: keep-highest is `d6 + d6` only under `use-cairn-dice-notation` — with the setting off that string is ARITHMETIC, so the composer takes the dialect as an argument and the probe drives both by shadowing `game.settings.get` in-page (the setting is requiresReload; a world write would be the leaked-setting failure again), and a mixed-size sum must emit the pool form `{1d6,1d8}`, because both terms are bare dice and the rewrite would claim `d6 + d8` as keep-highest — the witness message shows the sum rolling `{1d6,1d8}kh`. Probe trap paid for here: a CLOSING DialogV2 lingers in the DOM, so reopening immediately hands the next leg the OLD dialog’s nodes with the OLD dialect in their closure — wait for the element to leave the DOM. **Since 2026-08-09 it also witnesses the WHOLE-LINE card keys** (review #13 #10): every detail-card line and status bar goes through `CAIRN.DamageCardLine` / `CAIRN.StatusBannerLine` whole — bold and colon included, faction-generator.js's own rule — rather than `'<strong>' + label + '</strong>: '` assembled in code, which hands a translator the nouns and keeps the punctuation. English output is byte-identical (asserted unshadowed: colon outside the bold on card lines, inside it on the banner — each key preserves its surface, and `dev:enc-damage`'s breakdown legs pin the Damage line's exact bytes), so the witness SHADOWS each key to a reordered `ZZ-LINE {value} ← {label}` form in `game.i18n.translations` (NESTED — lang JSON is expandObject-ed on load, localization.mjs:368, so get/setProperty) and drives real cards: the hazard card, the combat card (`_showDetails` is a separate method whose lines could regress alone), the chat bar, AND the sheet's banner — npc-sheet.html renders the SAME `CAIRN.StatusBannerLine` through the `localize` helper's format-args form (handlebars.mjs:265-268), one key being what keeps a translator's reordering from splitting chat from sheet. Witnessed: reverting the four code files reds exactly the four key legs ("NOT FOUND — the line is still concatenated") while the byte-identity leg stays green — the old concatenation produces the same English bytes, which is exactly why a bytes assertion could never witness this fix. Probe trap paid for AGAIN: the sheet leg first read the WORLD actor's sheet while the hit had paralyzed the unlinked token's SYNTHETIC delta actor — no banner, empty string; the unlinked-token trap's third catch in this repo. Needs `npm run dev:players` (Alice). |
| `npm run dev:combat` | **Cairn’s turn order in the tracker** — no initiative queue: each party member DEX-saves at the start of combat, pass acts before the enemies, fail after, order inside a bucket is table talk. Stored as initiative **1 / 0 / −1** (core’s descending sort orders the buckets by itself; worlds already hold these values, no migration). **Deterministic without stubbing a die**: DEX 20 cannot fail a d20 save and DEX 0 cannot pass one, so every bucket is forced by fixture. Legs: the save card is an HONEST save (“DEX save 8 vs 20: acts before the enemies”, never core’s “rolls for Initiative — 14” — the right die wearing the wrong label); a **NEUTRAL-token PC still saves** (`isPartySide`: a character-type actor saves regardless of disposition — the old code read disposition alone and sent a disguised PC to the enemy slot); the enemy takes initiative 0 with **no roll and no card**; a hidden combatant’s card is GM-whispered while a visible one’s is not; **ONE batched `updateEmbeddedDocuments` and zero `Combatant#update` calls**, counted by wrapping both during the roll — the fork-era shape was super-then-patch, one batch plus N awaited updates each re-running `setupTurns`; and `combat.turns` reads bucket-sorted with **unrolled combatants LAST** (null sorts −Infinity — visibly unrolled, distinct from failed). The tracker (`CairnCombatTracker`, `CONFIG.ui.combat`, one forked template part) shows **three dividers in Cairn’s order** (Act first / Enemies / Act last), a **word on every rolled row and zero numeric `initiative-input`s** (1/0/−1 is an encoding, not information), and the roll button kept on unrolled rows — the empty slot is the invitation to save. **The `null <= DEX` leg runs as ALICE**: she rolls her own combatant plus an id she does not own — hers lands (DEX 20), the unowned one is SKIPPED and stays null. Core skips combatants the caller does not own (combat.mjs:402); the old post-processing loop then read the skipped combatant’s null initiative, and `null <= DEX` is TRUE, so a save that was never rolled counted as a pass. Needs `npm run dev:players` (Alice). **Since 2026-08-08 it owns Warden drag-to-reorder within buckets** (real DragEvents on the real rows, so the listeners are the thing tested): rolled rows draggable and unrolled not, a drop moves the row and renumbers the whole bucket 10/20/30 in ONE batch (`combatSort` flags — the drag WRITES and the render re-asserts, because `enforceTurnOrder` would un-do a DOM-only move), the rendered rows follow the write, a cross-bucket drop refuses with the `CombatDragBucket` toast and changes nothing, an unrolled row is not a drop anchor, a second client reads the same order off the document, and **mid-combat a reorder keeps the active combatant** — `setupTurns` keeps `turn` as a numeric INDEX, so the drop handler restores it by id; the leg polls for the ORDER change, never for "B is active", because B was active before the drop too and the first run of this leg read `combat.turn` mid-flight. **Since review #13 it owns the POPPED-OUT tracker's styling**: the popout's root id is `combat-popout`, not `combat`, so the `#combat`-scoped divider and bucket-word rules never reached it and the popout rendered them as bare unstyled rows while every sidebar leg stayed green — the rules are `ol.combat-tracker`-scoped now, like the drag rules always were, and the leg reads the COMPUTED display/font-variant inside the popout (witnessed: selectors reverted to `#combat` red this leg alone, `display=list-item caps=normal`) |
| `npm run dev:print` | **the printable character sheet** — a detached sheet prints only its displayed tab, so the Print frame button (characters only; the npc sheet must NOT have it) opens one standalone page holding the whole character in Kettlewright’s /print/ layout and offers the browser’s print dialog. The popup is driven by wrapping `window.open` in the OPENER before the click and stubbing `print` on the returned Window before the page can call it — a stub attached on the popup event would race the call it observes — and the stub records what the document held AT CALL TIME, which turns “print fires after the page is built” into a deterministic claim: the witness that calls `print()` before `document.write` reds it with `sections: 0` on an empty document. Legs: **Description AND Notes both present** (the both-tabs leg — the reason the feature exists; the witness drops the Notes section from the template); bonds and scars carried; **stored features stay OFF the page** (the Features UI went 2026-08-09 — asserted against planted data, which must survive on the document); a **disabled omen OMITTED** with its text still on the actor (empty sections are dropped, never printed as placeholders); traits composed to prose age included by the sheet’s own `_buildTraitSentence` — never a second composer; KW’s item annotations `(d6)` `(3 uses)` `(petty)`; a **connected container as its own inventory section** (“ZZ Print Sack ( 1 / 4 )” with its item); an **injection leg** — an item named `<img onerror=…>` renders as ONE text node, zero elements, payload unfired, and the witness that switches the row to triple-stash reds it with `["IMG"]` and the payload FIRED in the popup; and **black-on-white with the opener genuinely theme-dark** — the page is the ONE surface exempt from the dark-mode token rule (docs/theming.md records it), and this leg is what enforces the exemption. Round-6 play-report additions (2026-08-08): the **background’s own prose** (planted background document, never pack prose); **Q&A as SEPARATE paragraphs** (p.q/p.qa pairs — never Kettlewright’s single blob; the witness merges them); the **source parenthetical, italic, under the name**; a **Connections section** — a companion with its stat line (DEX 16) and description prose, a thing with the line only, and a 0-slot companion holding nothing no longer printed as an empty inventory heading; the **credits footer in very small type, crediting the art actually ON the page** — the portrait's PATH picks the attribution line (Aspeheim CC BY 4.0 / Tlomdev CC BY-SA 4.0 / Lydia Comer ALL RIGHTS RESERVED / game-icons CC BY 3.0), the Yochai Gal text credit always prints, and the legs assert the WRONG artist is absent on both the PC and monster pages (fixtures wear real gallery paths; the witness restores the static both-names footer and reds both); and a second print pass proving the **Notes header prints with Notes empty** (the ruling: the empty block is where the pencil goes) while **no connections means no Connections section**. The overlay-bypass fields the review caught (background name, container names, bonds, omen, scars) are routed like the sheet’s. Also 2026-08-08: the **failed career under the background, Barebones only, labelled “Failed Career:”** (its world setting defaults OFF, so the leg shadows the settings READ in-page — never a world write); **Print on people and monsters too** (superseding the same day’s characters-only ruling — a monster prints its role where a PC’s background goes, statblock prose overlay-routed, none of the PC sections; a THING’s sheet still has no button); and the button sits **to the RIGHT of Pop Out wearing the visible word Print** — both title-bar rulings have their own legs and witnesses. And later the same day: **Kettlewright’s two-column band** — Stats+Items left; Traits, the background’s DESCRIPTION and Connections right; the **Q&A full-width below the band under its own Questions heading** (band legs read each column’s h2 list and the computed `display: grid`; the witness deletes the grid rule — and left its comment orphaned, the string-replace trap, restored under it); with nothing for the right column the band collapses to one column rather than printing a monster at half width. **Notes takes the MIN-ROOM rule on a character print** (2026-08-10, superseding 2026-08-08's always-fresh-page: `break-inside: avoid` + `min-height: 10cm` on the body-class-gated rule — Notes starts on the earliest page with ~10cm free and the pencil room is the rest of that page, moving whole to a fresh page only when less remains, so a boundary character never buys a near-empty page; the leg reads the COMPUTED values on both the PC page and the monster page — a monster's one-pager must stay one page and gains no min-height). And the source parenthetical says **“(Custom Cairn 2e)” by MEMBERSHIP** — the character stores contentSource "2e" either way, so custom = the resolved background living anywhere but the canon backgrounds-2e pack (the fixture is a world item, which is custom by the recorded definition); Barebones prints the plain label, proving the branch. **Since 2026-08-08 it owns the spellbook prefixes** (user report: the printed sheet dropped them): a bare-named book prints "Spellbook — X", a scroll — a flagged spellbook, never a type — prints "Spellscroll — X", and a stored name already carrying the prefix is NOT doubled, all through the same registered `spellbookPrefix` helper the inventory template uses, so the two surfaces cannot drift; the legs read the localized prefix keys, so they survive a translation. **Since review #13 it owns the ROUTE PREFIX**: the print page's `abs()` goes through `foundry.utils.getRoute`, because resolving against `location.origin` alone drops a server's routePrefix and prints every portrait and icon broken on a prefixed host — invisible on this unprefixed dev server where the two spellings coincide, so the leg shadows `ROUTE_PREFIX` in-page (restored in a finally) and asserts the URL STRING carries the prefix; a second leg proves an already-absolute portrait URL passes through untouched with the prefix still in force, since getRoute re-joins slashes and would mangle a scheme'd URL (its fixture updates the actor with `render:false` — the OPEN sheet re-rendering with an unresolvable URL lands a resource error in the watched opener console, a race the first run happened to win). Witnessed: `abs()` reverted to the origin join reds the prefix leg alone while the absolute leg stays green — `new URL` passes absolutes through under both codes. **Since 2026-08-10: Deprived/Panicked print as ALWAYS-PRESENT mark boxes** on a character page (user ask — the paper sheet needs somewhere to pencil a condition mid-session even when printed clean): empty on the clean pass-1 actor, pre-FILLED on pass 2 with both conditions set, ABSENT on the monster page (which keeps the text status line). **They ride the HEADER**, in the free height the 90px portrait reserves beside the name stack — under the stats they cost a row, and that row pushed a boundary character's Notes to a third page (user report, same day). **And the ATOMIC-ENTRY policy** (same day's ruling): every inventory row, bond, scar, connection and question-with-its-answer prints whole or moves whole (`break-inside: avoid`; the Q&A pairs gained a wrapper div so a question can never strand apart from its answer), and headings keep their content (`break-after: avoid` on h2/.inv-head). Witnessed in one run with `marks` nulled AND the pagination CSS commented: exactly the six legs red (boxes, header placement, filled, atomic, headings, min-room), everything else green. **And its spellbook fixtures plant under the GLOG-off settings-read shadow** (same day): in a GLOG world the create seam converted the planted "Detect Magic" to a scroll and redded the book-prefix leg — the dev:grimoire fixture corollary reaching its second probe; the world's setting is the user's, read-shadowed, never written |
| `npm run dev:companions` | **the mount role evolved into companion, and the canon’s prose companions are minted** (2026-08-08). The migration legs use the RAW socket, because a stored "mount" is unobservable any other way: migrateData rewrites the source at initialization, so even `_source.system.role` reads "companion" on a document the database still holds as "mount" — the fact that cost the world migration its first draft (it filtered on the stored value, which matches nothing, ever; the restamp is BLIND like the hireling one). Legs: a raw-planted "mount" READS companion and the restamp writes companion to the DATABASE (raw-read back); the sheet’s role select says Companion and never Mount; the pack is relabelled **Companions & Transports** with the folder renamed under its stable id; the **Falcon and Raven Familiar carry their whole stat blocks** (3 hp 5/16/4 and 8 hp 3/11/13, slots 0, their own glyphs); the falcon option grants the companion and **the raven is an Actor grant NOT an item** with the background-items orphan retired (the Outrider precedent), while **Mountebank’s tattoo stays prose** — the ruled boundary, and the control proving the reader distinguishes; a GM grant mints the Falcon **with DEX 16, not the schema’s 10** — the abilities-copy leg, nothing else can see it (grantContainers copied only hp/armorOverride, review #5’s bug class); and **Alice’s grant crosses the broker** as a companion she owns — the probe’s FIRST run caught the broker’s payload whitelist silently rebuilding her raven as 10/10/10 while the GM path copied the stats, and the GRANTABLE_ROLES witness hands her a CONTAINER-role raven via the derive-and-clamp fallback. Probe traps paid for here: a witness that makes a read THROW must be caught into the leg or it kills the run before cleanup and strands an INVALID document (unreadable by the sweep that cleans by name); and ownership is polled WITH the mint — the broker creates first and writes the ownership shape second, so reading isOwner the instant the actor appears races the second write. Needs `npm run dev:players` (Alice). |
| `npm run dev:delete-confirm` | that an inventory row does **not** vanish before the Warden has answered "Delete X?". The delete helper awaits a confirm dialog, and the handler used to slide the row up on the same tick — so declining left the item in place with its row already gone. Three independent readings taken *while the dialog is open*: `Element.prototype.animate` wrapped in-page and counted on the row (the slide, measured directly rather than inferred), the row's `offsetHeight` sampled every 30ms for twice the slide's length, and `row.isConnected` (the slide's callback re-rendered the sheet under an unanswered question). Each pass is then answered both ways — decline must keep the item, accept must really delete it, or "nothing vanished" would pass on a control that does nothing at all. Three reds pre-fix (a feature row ran the same two passes until the Features UI and `deleteOwnedFeature` went, 2026-08-09) |
| `npm run dev:container-link` | container linking **as a real player** — a GM passes every ownership check *and* every Warden gate, so only this catches permission bugs. A player dropping a Warden-owned mule onto their own sheet must be refused, told why, and leave both documents untouched; a legitimate purchase must come out fully linked, owned, and — since 2026-08-01 — wearing the CONNECTED ownership shape via the GM relay (default OBSERVER + buyer OWNER, polled to settled, not the first observable); the buyer's row now OFFERS unlink (she owns both ends under the shape) while trash stays the Warden's — Actor deletion is role-gated with no player-grantable permission, and the GM's trash is what keeps her missing one load-bearing. Ends on the derived list: `connectedTo` alone puts an actor on the tab, clearing it removes it, deleting it makes it vanish, and **`system.containers` must not exist in the schema** — a stronger statement than the "the legacy array is empty" it replaced, since it fails if anyone re-adds the field. **Parked Connections UI (2026-08-09)**: the drop leg and the edge-control legs run under the in-page read-shadow (the drop would otherwise refuse silently at the parked gate, before the ownership wall the leg exists to witness); the purchase flow needs no shadow — it mints via creation data. **The buy leg runs under a read-shadow forcing `allow-player-marketplace` ON** (2026-08-10): the dev world keeps the switch off (the Warden's table choice), `acquireTransport` reads it live on the calling client, and without the shadow the purchase refuses at the shop door — five legs red on world state, not code; the switch-off refusal itself is `dev:marketplace`'s differential |
| `npm run dev:item-pile` | the container class label and Item Piles. Walks **every** shipped transport asserting its label and its art agree (they come from one classifier, so drift shows as "Horse" beside a picture of a cart), then the other half of the classifier — **ten names with no stored class**, which is what a Warden typing one in gets: purpose before construction (`Burial Wagon`/`Hearse` → **wagon** since the funeralwagon class retired 2026-08-02, the rule surviving so a Hearse never falls to the chest; `Funeral Cart` stays a cart), and the two word-boundary rules, since "Draft Horse" contains a raft and "Handicraft" a craft. Both witnessed: strip the rules and six names fall back; write them as bare substrings and the two boundary cases sail. The Burial Wagon pack doc is the drifted-filter's DELIBERATE exemption — stored class wagon under shipped coffin art, the decoupling's shipped witness. Then makes a pile — an npc whose **Kind** is `pile`, since a pile is neither a document type nor a `transportKind` any more: class art on creation, re-arting when the Kind changes, hand-picked art left alone, the sheet's **Type select** (selected label, the role's options plus Other…, unclipped), directory visibility, and — **as Alice** — that a pile refuses a player with no rights and works once granted them. Rewriting this leg off the retired `transportKind` pick-list is what exposed the `migrateData`-over-diffs bug now gated by `dev:role-migration`. Last (2026-08-06): **Fatigue never lands on a thing** — six legs, because one npc sheet serves all five roles and used to pass `withFatigue=1` flat, so a Warden could give a sack Fatigue. The header must be absent on a container **and present on a character** (every other assertion here is an absence, which passes just as well when the selector is wrong); the enforcement is driven by PLANTING the button the old template rendered and clicking it, which is not a contrivance but exactly a sheet left open while the actor's role changed; the drop route is exercised separately because hiding a button does nothing about a drag; and the character end must still gain Fatigue, or the refusals prove nothing. **The middle leg is the one to keep**: `withFatigue` also gated the COIN-SLOT rows, so the naive one-flag fix silently deletes them — the leg gives the container gold and requires `.gold-slot-row` count to equal `goldSlots`, and it goes red (`rows=0 slots=2`) under exactly that naive fix. **The scroll-identity legs run under a settings-read shadow forcing GLOG OFF** (2026-08-10): in the GLOG dev world the create seam turns the planted plain book into a scroll at birth, so the first drop merged LEGITIMATELY — reading exactly like the review-#9 regression the section guards against |
| `npm run dev:sheet-ids` | per-window DOM ids, via a real label click |
| `npm run dev:sheet-basics` | that editing a field commits with no save button (text, number, `<select>`, `<textarea>`), that one change makes exactly one update, and that each tab shows one panel and only one |
| `npm run dev:dialogs` | the sheet dialogs (add item and the regenerate confirm — the add/edit-feature pair went with the Features UI, 2026-08-09, and the probe now asserts the Features section is **ABSENT** on the PC Description tab against a PLANTED stored feature, which must also survive on the document) — that `button.form` reaches the right fields, the content templates carry no nested `<form>`, the confirm still defaults to No, and that Add Item **opens on** the right type. Plus the Create Actor SWITCHBOARD (2026-08-02): `CairnActor.createDialog` replaces core's type-picker on the world path, so the probe asserts the six role choices render for a Warden, no `select[name="type"]` exists there, `hireling` is unmintable by construction (roles, never types — `abHideHirelingType` is deleted and asserted UNREGISTERED, its withHookOff control replaced by these structural legs), the default is Player Character, and dismissing creates nothing. The custom-container dialog and its leg are GONE (2026-08-01, with the flat graph) — its real coverage, the Kind→art→label chain, lives in `dev:item-pile`, and `dev:ui-parity` asserts the link is absent from the tab. **Since 2026-08-07 it owns Impaired / Enhanced damage** — Cairn's replacement for advantage: a roll is standard (the weapon's die), impaired (1d4 whatever the weapon) or enhanced (1d12 whatever the weapon), chosen on the damage click. Run with **`use-panic` OFF, and that is a requirement of the design rather than probe tidiness**: the only d4 substitution that existed before this lived inside panic's branch gated on that setting, so a version built by extending it would vanish for a table with panic off — if these legs only pass with panic on, the seam is wrong. The three buttons render in order, the **middle one shows the WEAPON's die** (a d6 weapon offers d4 / d6 / d12), a click resolves to its action, each choice maps to its formula, and end-to-end through the real action an enhanced roll produces a **1d12** card with the badge. Asserted on the ROLL, not the badge: under the neutered helper the card said "Enhanced" over a 1d6, so a badge-only leg would have stayed green with the rule broken. Control: the same weapon rolled standard keeps 1d6 and carries no badge. **Dismissing resolves `null` and rolls nothing** — the leg that catches DialogV2's null-callback trap, since a button callback returning null resolves to the ACTION STRING instead (dialog.mjs:273); witnessed by making dismissal default to "standard", which reds both dismissal legs. The probe now carries a `watchdog` and races every dialog await against a timeout, because a promise from a dialog that never opened never settles and `await` on one hung the run for 15 minutes with no output. It also gates each leg on the previous dialog being **gone from the DOM**: a closing DialogV2 lingers for its animation, so a poll run straight after a submit finds the PREVIOUS dialog and every click lands on a corpse — that is what made the dismiss leg report UNSETTLED while the ✕ worked perfectly in isolation, and it silently turned the leg after it into a false pass. Last and deliberately last, so the setting cannot leak into the legs above: **panic imposes impaired and asks nothing** — with `use-panic` ON and the character panicked, NO dialog opens and the roll is 1d4. Both halves are asserted, because the die alone would pass on a build that still asked and then ignored the answer, and the no-dialog check uses a SHORT wait rather than the 6s poll the other legs use — a long poll would pass just as well on a dialog that was merely slow. The badge is read from the **`.dmg-quality` element**, not from the whole flavor: the weapon sentence already ends "(Panic)", so a flavor-wide regex stayed GREEN in the fail-witness with no badge at all — and the badge matters precisely because the attack line REPLACES that sentence whenever there is a target. **Since 2026-08-07 the dialog is decorated, and each part is its own leg.** Every button carries **its own die's glyph**, read as the `<i>`'s class list rather than as a rendered glyph: unlike the chat control's `fa-burst` the claim here is that d4/d6/d12 DIFFER, and every `fa-dice-dN` renders something, so a "draws a glyph" check would pass on a helper returning d20 three times. The **fallback is asserted at both ends inside one dialog** — `askDamageQuality("3")` must leave the middle button with NO icon while impaired and enhanced, whose formulas are constants, still carry theirs; either end alone passes on a helper that always returns "" or always returns something. (`\bd(\d+)` was the trap: there is no word boundary between the digits and the `d` in `2d6`, so it matches nothing on the commonest multi-die formula.) The **default cue** is read three ways at once — the class, the `autofocus` attribute it claims, and the computed weight — because `default: true` emits ONLY `autofocus` (dialog.mjs:242) with no class and no styling hook, and core ships no `button[autofocus]` rule, so a class present with no rule behind it is invisible to a player; the CSS witness returned `class=true autofocus=true weight=400`, which is exactly that failure. And the footer computes **`flex-direction: column`**, core's row rule being `@layer blocks.base` (foundry2.css:5478) against this file's `@layer system`, so the stacking needs no `!important`. Two witnesses, disjoint: neutering `dieIcon` reds the two icon legs while order, default-cue and stacking stay green; disabling the CSS block reds the cue and stacking legs while the icon legs stay green. **The word is "Standard", not "Normal"** (user ruling 2026-08-07) — the i18n key and the button's `action` string were renamed with the copy, so `dev:review-batch2`, which drives `rollDamage` through this dialog and answers it, selects `data-action='standard'`; a changed button set breaks that probe silently and it must be run alongside this one. **Since 2026-08-07 STANDARD LEADS** (user ruling): the order is standard / impaired / enhanced, so the leg's NAME and all three of its indices moved with it — getting only the actions array right leaves it passing while asserting the wrong button, and `oddIcons`' null moved to index 0. Beyond the ask this closes a latent trap: `isDefault` falls back to `(i === 0) && !buttons.some(b => b.default)` (dialog.mjs:228), so with Impaired at index 0, dropping `default: true` would silently have made IMPAIRED the button Enter presses. The **title names the item** the roll came from, asserted at BOTH ends — `Damage roll — <item>` with one, the plain title without — because asserting only the named form passes on a build that always appends, and a dangling "— " is not repairable by a translator (hence two keys, not one with an empty placeholder). **Nothing is pushed to an edge** (user ask): the button computes `justify-content: center` and the ⏎ pseudo-element's `margin-left` is read as a NUMBER under 20px rather than as `!== "auto"` — the witness showed getComputedStyle resolving the old auto margin to **109.969px**, so a string comparison against "auto" would never have matched and the leg would have been green with the offset live. The probe states its own limit here: a pseudo-element has no rect, so whether the result LOOKS centred is the user's eye, not an assertion. **Since 2026-08-07 the QUESTION must agree with the buttons.** Moving Standard to the front left the prompt still reading "impaired, standard or enhanced" — the last place in the dialog stating the old order, and nothing could see it: `i18n:check` compares key presence, not values, and no leg read the prompt at all. The leg extracts the order the sentence names them in and compares it to the `actions` array rather than to a literal, so it cannot go stale the next time a button moves and it fails if any of the three is missing. Witnessed by restoring the old wording, which reds it alone while "three choices, in order" and "the FIRST button shows the WEAPON's die" both stay green — the demonstration that those two were structurally blind to the copy, which is how the drift survived a full round. |
| `npm run dev:theme` | that the sheets stay readable in **both** colour schemes: every text and border colour measured against its real backdrop, in light and dark. Light is the baseline, so it only fails on something dark breaks. `-- --shots` also writes the four screenshots |
| `npm run dev:sheet-layout` | that no two regions of a sheet grid overlap, on all four actor types. Generates **six** characters, because whether the layout fits depends on how long that background's description happens to be — one sample passes where six catch it. The item-sheet half covers all four spellbook states (book, scroll, glog, glog scroll) since the GLOG checkbox became the fifth counter: five and six counters both need three counter rows and one 5-row template, and the counter-visibility check is what catches a counter auto-placing into an implicit row under the tabs — confirmed red with the old 4-row CSS on all four fixtures. **The item plants run under a settings-read shadow forcing GLOG OFF** (2026-08-10): in the GLOG dev world the create seam converts the book and glog-book fixtures to scrolls, and the probe stays GREEN while silently testing the scroll layout twice and the book states never — a coverage loss no red would ever surface |
| `npm run dev:notes-editor` | that the Notes editor can be **typed into** and saves, on all three actor types, plus the empty-field placeholder. It types with the keyboard and touches no button, because the regression it exists for left the editor present, upgraded, `contenteditable="true"` and holding the right value — while being 0px tall and therefore unclickable. Since 2026-08-02 EVERY notes editor is **toggled** — the npc pair first, the character's later the same day (the user reversed the recorded always-active decision), so the character joined the toggled loop and the always-active section is gone — asserting the `toggled` attribute, the **display-half empty-state hint** as a real element (`.cairn-editor-placeholder`, because the `::before` mechanism has no `.editor-container` to anchor to before activation) with **monster wording on a monster**, pencil-open + type + click-away save, and a re-open where the display half serves the prose and the hint is gone. Also covers three **item** sheets, typing into the toggled description editor and then closing the sheet — the actor loop had left every item sheet untested. **Since 2026-08-08 it owns discoverability**: the INACTIVE notes editor reads as a field (computed border + wash on `prose-mirror.inactive`, notes tab only), the pencil is visible **unhovered** in the palette colour (core keeps it `display: none` until the editor is hovered — the colour compared against the token the page resolves, so scheme-agnostic), a click on the EMPTY display area activates the editor through core's own toggle button, and with content present a click on the text stays inert (selection and links keep working) while the pencil still opens it. The placeholder copy is the call to action ("Click here to add notes…") — an en.json VALUE change, so Spanish drifts until the next handoff. Two disjoint witnesses: dropping the click binding reds the click leg alone; disabling the field-look CSS reds the field leg alone while the pencil leg stays green |
| `npm run dev:phantom-save` | the phantom save in the **all-toggled era** (re-keyed 2026-08-02): the original defect — a pristine ALWAYS-ACTIVE editor's `save()` firing on a serialization diff, writing the document from a mousedown and eating the click under the re-render (the 2026-08-01 "art picker never opens on Acolyte" bug; 23 shipped monsters rewritten in one evening) — lost its last shipped surface when the character Notes editor went toggled (`c873bd6`), the npc pair having gone at `8c67e8e`. The probe sat **stale-red** from `8c67e8e` until the same evening, having been absent from that batch's run list. What it pins now: **every `<prose-mirror>` on the sheet is toggled and none is active at rest** (the leg that keeps the phantom class empty — re-ship an always-active editor and this reds, forcing the question again; the `bindEditorClickAwaySave` veto still ships, guarding that empty class); a held 200ms press on the RESTING sheet opens the art picker with `modifiedTime` untouched; closing the resting unedited sheet writes nothing; an ACTIVATED editor's click-away **commits by contract**, converging the imported non-canonical shape once, with `&` still stored as `&amp;` (the corrected convergence claim); and **convergence is a fixed point** — the same activate-and-press gesture on the now-canonical document writes NOTHING, because if `modifiedTime` moves there, the serializer and the sanitized store have stopped agreeing and the once-per-document write has become a write-per-click loop. Deliberately NOT asserted: the click-death itself (headless Chromium re-targets a click whose mousedown element was re-rendered away). Typed-text saving stays `dev:notes-editor`'s coverage |
| `npm run dev:header-buttons` | the inline title-bar buttons — Roll Character, the Randomization toggle, and Pop Out: labels, state (the toggle hides Roll Character and relabels itself), that an unrelated re-render leaves them alone, that no label is clipped or escapes the header, and that ⋮ sits to their right. Pop Out is clicked **for real**: it must open a browser window, move the sheet into it, hide itself, and come back on re-dock — the settle time varies 1–2.5s, so every wait polls the condition rather than sleeping. Since 2026-08-02 it is also the home of the **default-Off leg** (a generated character and a bare hireling open with the toggle reading "Randomization: Off", Roll hidden, no sheet-body dice — control: stash `module/data-models.js` and the default reads On again), the **"Roll Monster" face** on a role-monster npc (`fa-dragon`, the directory button's icon), and the **hard-lock guard**: with `show-generate-header` off the Roll button is not built at all but the toggle STILL is, because it is the flag's only user-reachable writer and hiding it would make Off permanent — the setting is flipped and restored inside one evaluate's `finally`. The **thing leg** (also 2026-08-02): a mount seeded `generationEnabled: true` builds NEITHER button and renders no body dice — the role gate must beat the stale stored flag, or a thing's dice would be permanent with their off-switch gone |
| `npm run dev:ui-parity` | the character sheet's stat-block **geometry** and computed styles — counter alignment and spacing, corner radii, button chrome measured against a reference button, that the custom checkboxes draw **one** box (core draws its checkbox with pseudo-elements, which `appearance: none` does not remove, so its glyph renders inside ours), the Cairn badge, the Connections tab's removed links (no market link, and since 2026-08-01 no custom-container link either), the settings sheet's positional grouping, and a sweep for untranslated `CAIRN.*` keys on every tab of a 2e *and* a Barebones character. **Since 2026-08-08 it owns the Deprived tooltip swap**: Rest/Restore disable while deprived and their tooltips swap to DeprivedTip (RestTip/RestoreTip otherwise), with a REAL mouse hover asserting the tooltip actually fires on the disabled button — the leg that also guards the engine assumption (disabled controls do receive pointerenter; verified Chromium + Firefox). **Since 2026-08-08 it also owns the Barebones header sizing**: the three header lines (Background / Failed Career / Failed Career Item, both halves each) share ONE computed font size — asserted as equality across the six elements rather than a literal 17px, so a future resize ruling moves one number in cairn.css and the leg follows; collected before the settings restore, because `showFailedCareer` re-renders away with its setting. **Parked Connections UI (2026-08-09)**: the tab/Add-Connection legs run under the in-page read-shadow, and the settings block asserts the parked default — derived flag false, tab absent, `connections-ui-enabled` registered `config:false` (a Warden-visible toggle would re-litigate the `show-containers-tab` removal) |
| `npm run dev:parity` | the failed-career field (re-roll, live setting) and the Omen field being 2e's alone — absent on a Barebones sheet unconditionally (the `show-omens-barebones` lending was removed 2026-08-09), present on a 2e sheet as the differential |
| `npm run dev:grant-hint` | the grant-source tags footer hint follows its setting |
| `npm run dev:portrait-folder` | the custom-portrait setting takes effect with no reload. Since 2026-08-09 it also asserts the **scan toast's plural** (review #13 #13): with exactly one image in the folder the toast must read "1 image found." — the `_one` form picked by `formatCount` — where the old key hardcoded an "image(s)" hack and a reverted call formats the bare plural ("1 images found."); neither failure shape contains the asserted substring, so one `contains()` reds both (witnessed: reverting the settings.js call alone redded this leg with the bare plural). Probe trap: the CLEAR step's toast lands after its list write, so the wrapper polls for the rescan's own `/1 image/` toast rather than the first to arrive — the first cut captured the previous step's "0 images found." |
| `npm run dev:directory-buttons` | the Actor Directory's creation surface, docked **and** popped out, at three permission levels (rewritten 2026-08-02 — its old counts were STALE-RED, saying 4 while a Warden had had 5 since the faction button, because the probe was not in that batch's run list): a Warden's **8** buttons, an ACTOR_CREATE player's **5** (no Monster/Faction/Import; the grant is established first, dev:monster-gen's pattern, or every player leg is vacuous), a bare player's **exactly one** (Generate PC — read after a reload, the honest model of a permission change reaching a player) — and **core's Create Actor button gone for all three**. The bare player's button is the **generatePC relay** (2026-08-02): the source picker answers on HER client, the active Warden's client mints with the requester stamped OWNER in the CREATE data, and the pcGenerated answer opens the sheet on her screen — plus the guard leg (activeGM stubbed null → a warning and no emit) and an exactly-one leg whose red under a live Warden session is the standing live-GM confound (two clients of one GM user both answer), not a regression. Its count legs tally **characters**, not all actors, so a live session minting NPCs beside the probe cannot red them. Each role button is driven through the real name+Type dialog to a mint asserted whole (role, kind, default slots, class art, unconnected); the **folder "+"** must survive, open the role SWITCHBOARD (`CairnActor.createDialog`), and its container path must mint INTO the folder — the folder-threading witness. The **named-mount clone leg** (2026-08-02): Create Mount's Type select carries the pack's named horses as an indented group (plain Horse surviving beside it, `doc:` sentinel values), picking Heavy Destrier prefills the editable name and mints a world clone judged against the pack doc (hp 8 / armor 1 / 2 slots / horse / same art, unconnected) — and the **Other… reveal is exercised through a real change event**, because DialogV2 renders content as innerHTML and the listener wired on the original nodes was DEAD in the shipped UI until this leg caught it (the fix wires listeners in the dialog's `render` callback, per dialog.mjs:152-155). Its own negative control taught a second lesson: a `prompt()` dialog has no cancel button, so an error path must dismiss via the header ✕ and wait the form out, or the next leg feeds the stale dialog. Dismissing the content-source picker still creates nothing (issue #6). The negative-control lesson baked into both this and dev:dialogs: under stashed code core's MODAL opens where the switchboard was expected, and an unanswered prompt hangs the evaluate — the dismissals close whatever opened, raced against a deadline. Needs `npm run dev:players` (Alice). Since 2026-08-02 the named-mount clone leg also asserts it does **not** inherit the pack's `ownership` (pack 0 NONE beside clone 1 LIMITED, so the reading is about the clone and not about the pack) and that it records `_stats.compendiumSource` |
| `npm run dev:warden-rename` | the GM rename cycle across page reloads |
| `npm run dev:content-overlay` | the content-translation overlay — chat cards, marketplace headings, the background picker, and the background **drop confirm** (the one background-name surface that still formatted raw English, and the one that would not have come along when the content phase lands). **Polls** for the picker rather than sleeping at it: it waited a flat 700ms, `promptBackground` grew to ~1.1s before it renders anything, and the miss was swallowed by an optional-chained `?.click()`, so the dialog was never dismissed and the probe HUNG instead of failing. Opens on a `picker rendered` assertion for that reason — a probe that reads an element that is not there yet reports every field as `undefined`, which is indistinguishable from a translation bug. Since 2026-08-02 it also covers the three surfaces a translator reported as untranslated with every cell correctly filled: the inventory row's expanded **description panel**, the **Scars** checklist (name *and* the per-row detail, a new `table.resultDesc` namespace), and the **item sheet while EDITABLE** — the only sheet a player who owns the item ever opens. Each is paired with its stored-value invariant (the scar checkbox value, the editor's `value`, and the document's own name/description after the sheet closes), because a translation that reaches the document is worse than one that never renders. The npc window-title leg expects the **role-prefixed** shape since 2026-08-02 ("NPC: \<display name\>" — the prefix is the Role select's word, not core's type label), and the same block asserts the description content reads at **16px** (the 2026-08-02 bump; core's default is 14). Since 2026-08-02 it also covers the **Create Mount clone group**: the six named horses render through `monster.name`, the option VALUE stays the `doc:` sentinel, and the name prefills with what was READ rather than the English behind it. **Round 5 (2026-08-04) is the NAME INPUT itself**, reported by fsmalecho as "item titles are still not changing": the window title localized while the field under it rendered raw English — "Arma: Daga" over a box reading "DAGGER", the same one-sheet-two-answers tell as round 1. Round 1 had recorded that this field deliberately stayed English *for want of a display/value split*; round 2 built that split (`sourceOf`), and the entry was never revisited — **an expired reason reads exactly like a current one**. Both ends are asserted on both sheet families: the field shows the translation, an edit to an *unrelated* field stores the ENGLISH source (the path `submitOnChange` takes on every keystroke — this is the leg that would catch a Spanish name being written onto a document, which breaks `resolveGearItem`, background grants and `check:refs`' 394-name assertion), and a genuine rename still lands verbatim because it misses the reverse lookup. **The PC legs are the ruling, not decoration**: a character's name is player-authored and is never localized, and the probe proves it by naming the PC *exactly* what the overlay translates — a PC absent from the namespace is left alone by `t()` anyway, so a differently-named PC would pass with the gate deleted. That gate was load-bearing on arrival: with the fixes stashed the control prints **"Player Character: ZZ-BESTIA"**, a real character renamed on screen by the overlay. Control: sources out, probe kept — exactly three legs red (both display halves and the PC title), the stored-value invariants staying green throughout, since they must hold before and after. **The review corrected the submit half the same day and the probe's overlay is now DELIBERATELY MANY-TO-ONE**, because the shipped one is (Lute and Lure are both "Señuelo"; seven collisions in the real es.json): a DECOY entry sharing each sentinel translation sits first in its namespace — the entry a reverse SEARCH returns — so the legs pass only under the ANCHOR submit ("does the value equal what the sheet displays for the stored name"), plus a typed-translation leg (typing another entry's Spanish stores VERBATIM — the "typed Escudo, got Shield" case) and a baked-name leg (a mount named in the display language by the clone dialog must NOT be un-baked to English by an HP edit). **Round 6 (2026-08-06) is the omen TEXTAREA**, reported by fsmalecho as "the Omens still aren't translating" with every translation present and `omenDisplay` already computed: the translated read span rendered only with the checkbox OFF, but rolling an omen requires it ON and unticking CLEARS the omen — so the translated surface was UNREACHABLE for any character actually holding one, and the round-2 span leg kept this gate green for two rounds by planting `omenEnabled: false`, a state no UI path produces. The textarea now shows `t("table.result", …)` with the submit anchored back to stored English in `_processFormData` (the fourth split; anchor ONLY, no `sourceOf` — an omen has no typed-label feature to preserve). Three legs: the textarea shows the translation, an untouched change-dispatch round-trips to stored English (its non-vacuity witness is the edit leg beside it — same event, same machinery, polled until the write lands), and a player's own words store verbatim. Controls run one half at a time: template reverted → exactly the textarea leg red, printing the reported English; anchor removed → exactly the round-trip leg red with the Spanish stored, the corruption the anchor exists to stop. **A probe that plants an unreachable state proves the template can render, not that a user can see it**. **The same day added the ROLL TABLE read surfaces** ("all roll tables needs to be fixed"): the draw card's `.table-description` header (new `table.desc` namespace — the extractor's old skip reason expired when v14's card started rendering the description above every roll) and the RollTableSheet's **VIEW mode** — rows through the shared `result-details.hbs` swap, the body `<h1>` and window title through `table.name` — with EDIT mode asserted to keep the English source (the read/edit split; the mode setter is STICKY session-wide, so the probe restores "view"). No sheet-description leg on purpose: core drops a root part's loose text nodes (`handlebars-application.mjs:213`), so a plain-text description never renders in view mode — the card is that string's only surface, and a leg for the sheet would be coverage theatre. Controls one half at a time: sheet hook disabled → exactly the three VIEW legs red; card swap removed → exactly the card-description leg red. The DATA half of the report was the bigger half: the Warden tables (monsters, factions/NPCs, travel, most encounters — ~530 rows) had never been translated because the translator's spreadsheet predated those packs — `i18n:extract` regenerates the hand-off with prefill, and no code change can display strings that are not in `lang/content/es.json`. **Since review #13 the Kind matcher accepts the ENGLISH label in a translated world**: the matcher's comment always promised "any language" while the compare ran localize() alone — active language only — so typing "Sack" on a Spanish client fell through to the verbatim-custom branch and silently lost the class's art and capacity (es.json also ships 14 of the 16 Class labels). The leg reproduces a Spanish client's exact shape in-page — the active translation for ClassSack made "ZZ-SACO", English stuffed into `game.i18n._fallback` (the same object core's fallback path reads, helpers/localization.mjs:394; empty on an English client) — and typing "Sack" must land the key `sack`; witnessed by dropping the English compare, which reds this leg alone with `stored "Sack"` while the active-label and verbatim legs beside it stay green. The connect-picker leg runs under the parked-UI read-shadow (2026-08-09) — the tab it lives on is hidden by default |
| `npm run dev:review-batch2` | review #6's data-integrity batch, one leg per finding: the **ACTOR_CREATE-player grant path** sets the ownership-sync flag and emits (the comment used to promise a GM sync nothing requested — polled to `default: OBSERVER` settled, as a real Alice with core permissions temporarily widened); the HP strip's **panic half obeys the use-panic setting**, matching prepareData's derived zero (setting off + still panicked must not leave HP silently un-editable — each phase creates its actor fresh under the toggled setting, the honest stand-in for the `requiresReload` reload); a **hireling-typed doc** (frozen npc alias) gets the capacity override, the Kind its rendered sheet shows (`system.classLabel` is deleted since batch 3 — the sheet's `kindDisplay` is the contract) and container art; a deleted keeper's orphans unlink in **ONE batched `updateDocuments`** (counted by wrapping the static, which both the old per-child `update()`s and the new batch route through); (the `createOwnedFeature` non-mutation leg retired 2026-08-09 — the method went with the Features UI); **two GMs, one portrait scan** (a second GM user is created, the folder is changed from the NON-active GM, and exactly one client may write `custom-portrait-list` — the same `Setting.create` race `migrateSettingsNamespace` documents); **read actions on a LOCKED pack** (rollDamage rolls, no PackLocked toast, while itemDelete still warns — the read set left `owned()`); and the background sheet's `_onDrop` **fires `dropItemSheetData`** and honours a false veto like core. The review's gear-glyph finding carries no leg and no fix, deliberately: core's own `nav.tabs [data-tab] > * { pointer-events: none }` (foundry2.css:5768) already makes the failure impossible, and a leg for it passed identically with and without a fix — fake coverage. Restores everything from Node: core permissions, pack lock, settings, the second GM, every document, and the scanned probe directory |
| `npm run dev:kw-traits` | Kettlewright import — traits and age |
| `npm run dev:kw-reroll` | Kettlewright import — re-rollable grants |
| `npm run dev:kw-guards` | Kettlewright import — refusals and guards |
| `npm run dev:site` | the landing page renders from `file://` with no broken images |
| `npm run dev:gear` | that **every gear name any grant path can hand a character resolves to a real pool item** — starting gear, choice-table options, bond payloads, hireling loadouts, harvested from the shipped packs in the running world. `tools/import/README.md` names this as the *only* protection the hand-maintained gear pool has, in place of an importer |
| `npm run dev:phase2` | that a generated character's gear is a live **copy of the editable pool item** — edit the pool, regenerate, see the change |
| `npm run dev:marketplace` | the shop is a **reference catalog** over that same pool, plus the buy/take flows. Since 2026-08-05 it also owns the rule that **a full pack cannot shop**: at exactly 10/10 both Buy and Take are greyed, and — the assertion that matters — forcing the `disabled` attribute back off and clicking anyway is *still* refused, because greying is the affordance and `acquire` is the enforcement, and a dialog left open while the pack filled must not be a way through. Transports and petty items stay available (neither costs the buyer a slot; buying a mule is how you fix being full). Control: freeing one slot re-enables Buy and the purchase lands. Two probe bugs were found writing it, both worth knowing — `foundry.applications.instances` is a **Map**, so the `Object.values()` close loop was a silent no-op and left a second dialog answering every query; and the buttons must be read only after `refresh()` has run, which the header's slot readout signals because the template renders it EMPTY. Polling for `.mkt-row` instead reads every button in its template state, which is *enabled* whatever the pack holds. Since 2026-08-08 also **the shop Torch arrives 3/3** — the marketplace copies the pool item's `system` verbatim, so the leg pins the SOURCE data; generation masked the pack's old 0/0 through its `uses: 3` annotation, and the shop was the one surface where the real value showed. Runs against the REBUILT pack (the pre-fix pack was the leg's red state, observed before the build) |
| `npm run dev:transports` | the Mounts & Transports Actor pack is the shop's ONLY source (15 npc documents in 3 folders; the legacy `transports` Item pack is asserted gone); buying mints a connected NPC carrying the document's stat block; the worn/mount slot distinction; keeping is TYPE-gated at the till (a mule, a cart AND an npc porter all refused — the porter is the flat rule's marketplace fail-witness, with the in-page forced-predicate control); and the connection ceiling refuses a buyer at `maxConnections()` BEFORE gold moves, witnessed below the cap |
| `npm run dev:barebones` | Barebones generation follows the SRD procedure **and** goes through the same editable pool 2e uses. **Since 2026-08-08 it owns the failed-career collision hook**: a background change landing ON the stored failed career re-rolls the career (excluding the new background, so it cannot re-collide) and swaps the keepsake, silently; a non-colliding change leaves both alone. Control first, collision second, on one actor with a planted SENTINEL keepsake so "replaced" is observable whichever career the re-roll lands on. A hand-picked collision on the failed-career line itself stays allowed — the hook covers arrivals from the background side only. **Since 2026-08-09 it also proves the Barebones toggle reaches an OPEN sheet** (review #13 #19): `barebones-failed-career` is read in `_prepareContext`, so "read live" was only true on the next render until the shared `rerenderActorSheets` onChange fan; the leg flips it under a real open sheet and POLLS for the header block's transition both directions. (The two settings that shared that onChange body — `show-omens-barebones`, `show-bonds-barebones` — were removed 2026-08-09 with the lending they toggled: Barebones sheets never show Omen, generation never mints a bond, and the probe's bond legs now assert exactly that plus a planted legacy bond surviving.) Witnessed: stashing the fan reds the leg alone with `left=false` — the off-flip never reaches the open sheet (the on-half can be satisfied by the world's prior value, so the off-half is the load-bearing direction). `show-generation-rolls` deliberately has no fan — no sheet reads it at render; the read happens when a generation posts |
| `npm run dev:spell-pool` | the random-spell pool is **canon-only and index-first** (2026-08-05 ruling: random spellbooks/scrolls draw from `Spellbooks`, never More Spellbooks). The draw RESULT is correct under either implementation, so it counts **work done** the way `dev:compendium` does — zero unfiltered `getDocuments()` calls across 10 item draws and a 200-draw sweep, with an in-page control reproducing the old load-everything shape to prove the counter trips. Counting note that cost a first run: `getDocument(id)`'s cache-miss path is `getDocuments({_id})` (one document, the intended cost) — only unfiltered calls are full loads. The canon-only leg asserts its own precondition (More Spellbooks non-empty) out loud, because with nothing outside the canon pack "only canon came out" is vacuously green. **The draws run under a settings-read shadow forcing GLOG OFF** (2026-08-10): under GLOG `randomSpellbookDoc` deliberately swaps the pool to the GLOG packs with canon excluded, so the canon-only leg reds 0/200 on the world's setting while both rulings hold — this probe's subject is the 2e pool; the GLOG pool is `dev:glog-magic`'s |
| `npm run dev:glog-magic` | GLOG as an OVERRIDING setting (2026-08-05 ruling), both halves. Generation: with the setting on, every random draw resolves inside GLOG ∪ More Spellbooks with **zero canon-pack reads** (`getIndex`/`getDocument`/`getDocuments` all counted), every drawn spell — the book path included — lands as a scroll, and a named grant comes back wearing the GLOG wording with `glog: true`; with it off, the canon shapes byte-for-byte — the setting itself is the control, no source edit. The SWEEP, upgrade-style: plants every migration branch in canon state (world item, owned book, owned **spent** scroll, an unlinked-token **delta-only** book a `game.actors` walk cannot reach, a book in a **locked** world compendium, a no-counterpart custom spell, and a book under a canon name whose GLOG counterpart is **renamed** — the page's 100 is not name-for-name the canon 100: two renames alias via `GLOG_NAME_ALIASES`, two are genuinely different spells and convert form-only), flips the setting, and confirms each was **rewritten**: scrolls with the GLOG wording, the spent scroll still spent, the pack re-locked, and a second direct `runGlogConversion()` writing nothing (`_stats.modifiedTime` unchanged — idempotence is what makes the two-tab GM quirk harmless). The invariant scan then walks the sweep's whole coverage: no spellbook is still a book, no swappable name still carries its canon wording. Pre-flip, the same checker must REPORT the planted canon documents — the detector demonstrably fires, so zero afterwards means something. Every pre-existing spellbook is snapshotted to Node first and restored by targeted diff in a Node-level `finally` — restore updates **omit `system.scroll` when unchanged**, because writing `scroll: true` over a live scroll re-enters `_preUpdate`'s transition branch and would refill a spent user scroll. **Since 2026-08-09 it also owns the CREATE seam** (ruling: there are no spellbooks in GLOG — made when a Haste dragged from the GLOG compendium stayed a book): what ARRIVES after the flip converts too — a canon book created on a character lands as an **unspent** scroll wearing the GLOG wording, the GLOG-pack drag itself lands as an unspent scroll (the leg pins the outcome, not the mechanism, so it survives the pack shipping scrolls), a no-counterpart book converts in form only, and a **bound page arrives a page**; the sweep-side half of that exemption is asserted on `glogConversionDiff` as a **pure function** — running the real sweep against a defeated skip would be a real write against the shared world. The off direction is the plant itself: created with the setting off, the plants must arrive as books. A world that arrives **converted** (setting already ON) is handled, not refused — flipped off for the canon legs, re-converted by the mid-run flip, left ON as found |
| `npm run dev:spell-tables` | the shipped "Spells — Canon (1d100)" table and the Warden reseed (2026-08-05 ruling: ship maintained tables + a reseed action, because Foundry has no dynamic pack-backed table). The shipped table: 100 document rows, formula 1d100, and **every row's uuid resolves name-for-name into the canon pack** — row uuids embed the pack name, so this leg is what catches a future pack rename; its control is a deliberately-broken uuid reading as unresolved, proving `fromUuid` can fail. The reseed, **update-in-place**: a world copy gets a bogus row planted (asserted present — the precondition out loud), then reseeds from the canon pack and must keep the **TABLE ID** (the whole point — `@UUID` links survive), drop the bogus row, and land 100 alphabetical rows — alphabetical read by RANGE, not collection order, because the embedded collection does not iterate in insertion order and the draw only consults range. An **empty source refuses** before any delete: reseed can never mean empty. And the Tables-directory button renders for the Warden, opens the two-select dialog with canon pre-selected, and Cancel touches nothing — every DialogV2 path ends on a button press, because an unanswered dialog is a hang, not a failure |
| `npm run dev:npc` | a generated NPC matches its book statblock exactly, that **no Features surface renders anywhere on the sheet** (the Features UI went 2026-08-09; asserted against a planted stored feature, which must survive on the document — an empty list renders nothing either way, so only data makes the absence load-bearing), and its gear is a live copy of the pool rather than a second inlined loadout. Since the NPC-role sheet parity work (2026-08-01) it also owns the **person half** of the sheet: a generated NPC arrives with pronouns, an age and all eight traits filled (differential: a bare Create Actor npc has none, so the schema initial alone can't green it); every biography field — pronouns, age, each trait, scarEnabled and a picked scar — **round-trips through the real sheet** and survives a re-render, witnessed by an undeclared sibling key on the same write being dropped (the greens hang on the `NpcData` declarations, not on Foundry keeping whatever it's handed); **identity is kept by omission** — profession and name re-rolls leave sentinel-seeded pronouns/age/traits untouched while `regenerateNpc` replaces all three, the two directions witnessing each other; the biography block is **role-gated** — absent on a monster, a mount and a container-role npc, with an in-page control forcing `showBiography` onto a monster so the absence assertions demonstrably can fail; and the **career → day-rate autofill** — a known career name fills a still-zero rate (case-insensitively), never overwrites a non-zero one, loses to an explicit `dayRate` in the same update, and fills nothing for an unknown name, witnessed by shadowing the base class's `_preUpdate` onto the instance and watching the same write leave 0; the person's **notes tab reads plain "Notes"** (2026-08-08 — every npc role now shares the short label; the character sheet's "Background & Notes" half is `dev:ui-parity`'s) |
| `npm run dev:portrait` | generation assigns a shipped portrait **and its paired token**, a regenerate disturbs neither, and the picker's swap keeps the two in step |
| `npm run dev:settings` | that the settings are **reachable by a Warden** — a namespace naming no installed package renders them under "Unmapped": present in the data, invisible in the UI. Every other probe passes while that is broken. Since review #6 batch 3, also the rendered window: the three injected group headers must **follow core's search filter** — core toggles `hidden` on `.form-group` elements only (category-browser.mjs), so the `<h3>`s used to float over whatever rows other packages left visible. A no-match query must hide all three, a query matching one Inventory row (its own label, read from the DOM) keeps only that header, and clearing restores them. And **`boldPhrase` bolds the LOCALIZED product name**: a sentinel label + sentinel phrase translation are planted in-page, because an English world cannot otherwise see that the old English literal misses a translated label (the Spanish "Ofrecer trasfondos personalizados de 2e" never contained "Custom 2e") |
| `npm run dev:age-override` | the minimum-age floor binds in generation **and** in the sheet re-roll |
| `npm run dev:bg-picker` | the background picker across both editions, and that the swap is surgical. Since 2026-08-02 also the **bond clamp**: a Fieldwarden (two bonds, the book rule — pinned here, nothing else asserts the exact count) swapped onto Kettlewright keeps the FIRST bond only; the second's granted items are deleted and its gold refunded — control: stash `module/character-generator.js` + `module/actor/actor-sheet.js` and the stale second bond survives the swap again. Since 2026-08-02 it also owns the **Add-a-bond double-click**: two clicks in one tick, through the real control. The COUNT is only the ceiling (it reads 2 either way, because both handlers wrote a 2-entry array); the witness is that no granted item is left tagged `bond:<id>` for an id no longer in `system.bonds` — unreachable by the ✕ and permanent. Its precondition deletes the items its own array-trim strands, so the assertion is "zero" rather than "no worse than before". Since 2026-08-04 also the **per-background eye toggle**: the shipped custom pack renders as its own "Custom" section (last); disabling a canon and a custom background excludes each from the pool while the Warden's view keeps them greyed; the eye click re-enables live; the last enabled background refuses to disable; and Barebones rows carry no eye at all |
| `npm run dev:content-sources` | that the content-source toggles govern **generation**, not just the picker. Creates a real custom background in a world pack and walks all four toggle combinations, asserting *which background the generated character came out with* — because asserting pool size passes in 3 of 4 cases whichever code is in place, and asserting "a character appeared" passes in all 4. Also that homebrew-only with nothing authored yet does **not** quietly refill from the shipped pack. Since 2026-08-05 also **the floor**: switching the last enabled source off flips Cairn 2e back on via the setting's `onChange` (and only 2e — the others stay off) |
| `npm run dev:playergen` | the player side of Generate PC, driven by TWO clients (a GM context so `activeGM` exists, plus Alice — a GM alone cannot see any of this). The **roll-confirm** (2026-08-08): with one-or-zero content sources enabled a player's click raises Yes/No before anything rolls — No and ✕ each create nothing and emit nothing (the socket spy and a GM-side actor snapshot both say so), Yes proceeds all the way to a created character (via exactly one `generatePC` relay emit when Alice lacks ACTOR_CREATE); the Warden is never asked (`promptContentSource` resolves instantly with no dialog); and with two sources the source PICKER appears unstacked — the confirm must not double-gate it — with picker-✕ still creating nothing (issue #6 held). Source counts are forced by shadowing `game.settings.get` **in-page on the asserting client**, never by writing the world toggles (leaked-setting lesson, and `enforceSourceFloor` makes real flips order-sensitive). Cleanup is snapshot-diff from the GM client: a Yes-leg character arrives with whatever its background granted, so everything new is swept by id and named in the output. **And the Warden's switch** (`allow-player-generate`, the shipped macro's setting): both clients show Generate PC while on; a GM flip removes the button from Alice's ALREADY-OPEN directory live and restores it live (the onChange → directory-sweep → hook-reconcile chain — the injected section is not a template part, so without the reconcile a flip changed nothing); the Warden keeps their own button throughout; and the broker leg — a raw `generatePC` emit past the hidden button mints NOTHING and the addressed PcGenDisabled refusal reaches Alice, because the button-hide is the affordance and the answering GM client's guard is the enforcement (witnessed independently: `allowGen` forced true reds only the flip-off leg; the broker guard dropped reds only the raw-emit legs, minting through a correctly hidden button). **And the marketplace switch** (`allow-player-marketplace`, the second shipped macro's setting — its legs live here, not in `dev:marketplace`, because that probe is a single GM page and these need the two-client harness; the Yes-leg's minted character doubles as the owned sheet under test): the shop button shows on Alice's owned sheet and the shop opens while allowed; a GM flip removes the button from her open sheet live and restores it live; a dialog opened BEFORE the flip still cannot sell — Take force-enabled and clicked refuses with the switch message and creates nothing (the greying is the affordance, `acquire` is the enforcement; the force-enable is dev:marketplace's own technique, adopted after a plain click proved dice-decided — a randomly-encumbered character arrives with every Take greyed and a disabled button dispatches nothing); `openMarketplace` refuses Alice at the door with no dialog; and the Warden shops throughout. Witnesses disjoint: `withShop` forced true reds only the sheet flip-off leg; the `acquire` guard dropped reds only the stale-dialog leg — the Take goes through and mints an item past a correctly hidden button. **And the randomization switch** (`allow-player-randomization`, the THIRD Warden switch, 2026-08-09 — its shipped macro is "Toggle Player Randomization"): off hides the WHOLE surface from a player — title-bar Randomization toggle, Roll button, per-line re-roll dice — even on an actor whose OWN flag is on (the `_mayRandomize` derivation is render-only, no actor written), live in both directions on Alice's open sheet; the toggle action called past the hidden button refuses with the switch toast and writes nothing (affordance/enforcement split); **since review #13 the enforcement is the `mayRandomize()` action-map wrapper over all seventeen surface handlers** — only the two frame buttons carried the guard in-handler while every per-line die answered a call past its hidden control — and a second enforcement leg drives `rollBackground`, the widest of the newly-guarded dice (`changeBackground` rewrites background AND granted gear immediately, no confirm, which also makes its witness deterministic: wrapper dropped, the call writes and never toasts — witnessed live, "Tailor" → "Leech Collector", that leg red alone); the Warden keeps everything throughout, POLLED with an open-guard because the GM client's first cold render of that sheet outlives any fixed sleep (bit the leg's first run). Witnesses disjoint: `_mayRandomize` forced true reds the flip-off and enforcement legs; the wrapper dropped from an action reds that action's enforcement leg ALONE — the probe re-establishes the actor flag after the toggle leg precisely so one mutation cannot cascade. All THREE switches are captured first, **ESTABLISHED on** (never assumed — the dev world arrived 2026-08-09 with the marketplace switch off and every "switch on" leg redded on world state, not code), and restored to their captured priors in the Node-level finally |
| `npm run dev:bg-containers` | background-granted beasts and vehicles — minted as container Actors, replaced on regenerate, and a player's own container left alone. Since 2026-08-01 also the grant-side connection ceiling: past-the-cap grants are CLAMPED (the first specs land, the drop is announced) and a zero-headroom grant refuses outright |
| `npm run dev:changelog` | the manual-change chat audit log (`change-log` setting, Plan A item 6, 2026-08-08), driven by TWO clients — the one-poster gate is invisible with one browser, the whisper audience needs a non-observer, and a player edit must post from the player's client. Field seam: gold / a STR value+max batch (one card, two lines) / a trait / Panicked marked-and-cleared wording / a scar / a rest-shaped HP write (the feature add+remove leg went with the Features UI, 2026-08-09 — its ledger lines were dead code once nothing wrote the field), each exactly ONE whispered card naming the acting user, whisper ids exactly the owner + Warden. An ENCUMBERED actor's HP write logs the STORED values (review #13 #1: `_prepareCharacterData` pins derived `hp.value` to 0 under encumbrance or panic, and the ledger's first shipping diffed the PREPARED document on both sides — a Rest that took source HP 1 → 6 posted nothing; both halves read `toObject()` now, damage.js's pattern, and the leg's in-page precondition checks stop it passing on an un-encumbered witness). Item seam: add and remove through `_onCreateDescendantDocuments`/`_onDelete…`, with Fatigue in its own words. Item UPDATE seam (review #13 #20+#21): a uses tick through the REAL − button posts the whole-line `CAIRN.ChangeLog.Uses` line; the rollover click — uses crossing zero on a stack of two — posts exactly ONE card carrying the quantity line AND the uses line, which is what witnesses the handler's one-write merge (the pre-#20 double update was two operations, so the seam would have whispered two cards for one press of one button); a flagged quantity write and an unflagged RENAME both post nothing (quantity and uses are the whole update-seam vocabulary — equipped/rename/description are off the ledger by ruling, and the rename leg is what keeps that boundary from creeping). Suppression: an `abNoStatusCard` update posts nothing, and `regenerateNpc` — the real machinery path, deleteAll + create + update — posts nothing (the DAMAGE path is not driven here: its writes carry the same flag at source, damage.js:125/149/487, and the real Apply button is dev:enc-damage / dev:hazard territory). Audience: the hidden actor's card is invisible on Alice's client while her own actor's is visible. Setting off silences everything live (no reload — what the shipped "Toggle Change Log" macro relies on). Action headers (2026-08-08): Rest and Restore Abilities, driven through the REAL sheet button and confirm dialog on the player's client (the option lives in the sheet handler, so a leg calling `actor.update` itself would stay green with the handler gutted — the two-layer Fatigue lesson), each post ONE card led by a `.change-log-action` header naming the button; a hand edit posts NO header, and an update smuggling a real-but-unlisted key (`abChangeLogAction: "CAIRN.Gold"`) posts its lines with NO header — the `AUDIT_ACTIONS` Set keeps the header vocabulary closed. It is ADVISORY and trusts the acting client (review #13, user ruling): the userId gate means the whole ledger posts from whoever made the change, so a crafted client can withhold cards or send a listed key at will — the Set is a table aid on the marketplace's client-enforced footing, not a security wall, and must not be hardened into GM-side posting unasked. The confirm click targets only a dialog that appeared AFTER the button press: a closing DialogV2 lingers with dead listeners, and a click on a dead dialog's submit button is a native form submit — a page navigation (bit this probe live). Whisper look (2026-08-08): the rendered tile's computed background equals the resolved `--ab-whisper-bg` — never core's lavender — and its inset box-shadow carries `--ab-whisper-stripe`, comparing against the tokens the page resolves. **The whisper tokens deliberately have NO dark override** — core's chat tiles are parchment in BOTH client themes with dark text to match, so the theme-keyed dark values shipped for one evening as an illegible near-black card (user screenshot); the stripe is its own pinned token because `--ab-accent` flips to the near-black-backdrop teal in dark and the leg now reds on exactly that regression (witnessed: reverting the stripe to `var(--ab-accent)` redded this leg alone on a dark client). A token-relative assertion is structurally blind to a bad VALUE — the user eyeball in both schemes remains the real gate for colour taste. The run REFUSES with a plain diagnosis when a second client of the Gamemaster user is connected (a canary GM edit counting two cards) — every connected client of the acting user posts, so a stray tab reds half the legs as "got 2" — **and the SAME canary runs as Alice**: the GM check alone stayed green while the user's two tabs joined as Alice redded every Alice-driven leg "got 3"; a per-acting-user gate needs a canary for each user the probe acts as. Cleanup sweeps the three planted actors and every ledger card from a Node-level finally; settings ride `withSettings` |
| `npm run dev:bg-author` | the custom-background authoring sheet: array edits persist, drag-to-snapshot, and generation resolves a snapshot in no canonical pack. Also the custom-bond paths (a named world table supplies the text and grants no gold; an unresolvable name falls back to 2e rather than to nothing) and **drop-to-swap** — decline changes nothing, accept rewrites the inventory without pocketing the background document, and a Barebones character or an NPC refuses *without even asking*. Runs in 8 announced stages under a `watchdog`, because it once hung for weeks inside a single `page.evaluate` and printed nothing at all |
| `npm run dev:bg-tools` | "Test ×10" preview/linter and "Duplicate into my backgrounds" |
| `npm run dev:kw-import` | the Kettlewright importer end to end, through the real options dialog and file chooser — the sample is a STOCK portrait pick since 2026-08-02, so the run also proves `portrait17.webp` maps to the shipped tlomdev copy on portrait AND token, and a direct-converter leg pins the rest of the decision table: the placeholder and an unknown number keep the random-pair fallback, a custom absolute URL is used verbatim |
| `npm run dev:kw-import-data` | the same importer at the data level — field mapping without the UI |
| `npm run dev:directory-ui` | the Actor Directory's generate-character icon and grayscale container thumbnails — keyed on the ROLE since 2026-07-31, so this row is what proves the re-key landed (the old `type == "container"` test matched nothing once containers became npcs, and greyed nobody). `dev:transports` owns the other half: since 2026-08-02 every container actor is ALWAYS listed (`show-container-actors` removed by ruling — its leg asserts the setting is unregistered and that a worn pack and a mount are both visible) |
| `npm run check:item-usage` | (offline) which pool items are actually referenced by a consumer, and which are orphans |

Not tests, but useful: `dev:actors` and `dev:players` create fixtures, `unpause.mjs`
unpauses a world.

### The 18 orphans

Everything from `dev:gear` down was written, worked, and then **had no npm script**, so
none of it was in this document and none of it ran. Swept 2026-07-29. What eighteen unrun
probes were holding:

- **One real content defect.** `dev:gear` was red: `Hawk` and `Robes` — gear for Cairn 2e's
  Animal Handler and Scholar hirelings — resolved to nothing, so both generated a piece
  short. The pool has no importer *by design*; this probe is the invariant that replaces
  one, and it was not wired.
- **Four probes rotted by the ApplicationV2 port**, each failing in a way that reads as
  "no result" rather than "error". Handlers are now private statics reachable only through
  the `actions` map, so a probe must **click the element carrying the `data-action`** the
  way a user does — `sheet._onRollAge(...)` and friends no longer exist.
- **Two probes asserting a skin that was deliberately reverted.** `f00e72c` moved every
  non-sheet surface back to Foundry's own colours so they would be theme-aware. Asserting
  Alegreya on a chat card asserts a bug. Deleted one, trimmed the other, and wrote *why*
  into its header so nobody restores them.
- **One probe hanging the renderer.** A bare `generateCharacter()` falls through to
  `promptContentSource()`, a `DialogV2.wait()` that blocks for a human. Inside
  `page.evaluate` that never returns and Playwright eventually reports **"Target crashed"** —
  naming neither the dialog nor the wait. Pass the source explicitly.

Two traps worth carrying forward:

- **`el?.[0] ?? el` is wrong on ApplicationV2, and fails silently.** The compat idiom for
  "jQuery or HTMLElement" must be `el instanceof HTMLElement ? el : el?.[0]`. An AppV2 sheet
  root is a `<form>`, and **`HTMLFormElement` is indexed by its own controls** — so `el[0]`
  is not `undefined`, it is the first `<input>`. The reversed order hands back an input
  whose `querySelector` matches nothing, and every DOM assertion reads false with no error.
- **A probe that changes the world must restore it from NODE, not from inside
  `page.evaluate`.** `age-override-probe` set `min-age` to 99 to test the age floor and
  threw on the next line; its restore sat after the throw, inside the same evaluate, and
  never ran. The dev world kept a floor of 99, so **every character generated afterwards
  was aged 99 and the age re-roll looked broken** — it was flooring to 99 too, so the value
  never appeared to change. It surfaced hours later as a bug report against the system.
  An exception inside `page.evaluate` propagates into Node, so a Node-level `finally` runs
  where an in-page one is skipped: use `withSettings(page, fn)` from `lib.mjs`, which
  snapshots every world setting, restores whatever drifted, and prints what it put back.
  **Still unguarded** (top-level scripts, no enclosing try): `e2e-dialogs`,
  `e2e-directory-buttons`, `e2e-portrait-folder`, `probe-bg-tools`.
- **A probe can pass having exercised nothing.** `age-override` asserted the re-rolled age
  obeyed a floor of 99 — but generation obeys the same floor, so had the click done nothing
  the assertion would still have been true. It now asserts the control *exists* first. When
  a probe's precondition and its assertion share a cause, green means nothing.

---

## After publishing

Full procedure in `RELEASE.md` and the `/release` skill. In summary:

1. Install the **published** zip from the manifest URL into the `:30001` environment —
   not a local build. The point is to test what a user downloads.
2. Create a fresh world, boot it, and run `FOUNDRY_URL=http://localhost:30001 npm run dev:smoke`.
3. **Always** `npm run dev:players`. A test world must never be left GM-only, and
   ownership behaviour is only exercised with real player accounts.
4. Unpause.

### If the release carries a migration

Any `Hooks.once("ready")` migration, any change to shipped image paths, any pack rename.
A fresh world has no legacy documents, so it cannot exercise a migration at all — install
the **previous** version first, seed real content (characters with inventories, an NPC, a
container, a world item, an **unlinked scene token** — five distinct branches), then
install the new build over it and boot the same world.

Add a branch to that list for whatever the release's own migration touches. For the
`role` one, that is **a hireling with a day rate** — `npm run dev:role-migration` covers
it on `:30000`, but the seeded upgrade world should carry one too, because that is the
document whose sheet quietly loses a row. For the connections one
(`flattenConnections`, 2026-08-01), seed three more: **a hireling-typed document**, **an
npc-kept chain** (PC ← npc ← sack — the shape the flatten re-points), and **a
NONE-default npc** (the shape the ownership pass raises to LIMITED) —
`npm run dev:connections-migration` covers all three on `:30000`, but they are exactly
the documents a fresh world cannot hold.

Note what a RETIRED TYPE does to that recipe. Since the `container` type was removed
(2026-07-31) a document of that type has no data model to load against, so the previous
version can no longer seed one — "a container" above now means an npc with
`role: container`. Retiring a type is a one-way door for upgrade testing, and what made
it safe here is that no world held one: the built type migration had already converted
the dev world, and `:30001` is a fresh branch install (docs/npc-roles-plan.md).

**A new field whose default contradicts what existing documents should have needs a
migration.** Adding it to the schema is not enough, and this pass is the only one that can
tell the difference.

**Count live documents, do not trust file size.** LevelDB keeps deleted records until
compaction, so an 80 KB `actors.db` can mean zero actors — and a run against that world
passes while exercising nothing.

---

## Reading the results

**A probe that fails once and passes on re-run is a race, not a flake.** Do not re-run
and call it green. Find out what the first run raced against.

**And the rule runs in both directions: a probe reporting NOTHING HAPPENED is making the
same kind of claim, and deserves the same suspicion.** Both probes written for the
2026-07-30 review returned a clean negative on their first run, and both negatives were
artifacts of the probe — one waited 1.5s for a 5s operation, the other hand-built a
document without the `id` its lookup keys on, so the code under test bailed before
reaching the line being tested. Taken at face value either would have killed a real
defect, one of them a live player→GM XSS.

So: **a negative result from a probe you just wrote is evidence about the probe until you
have shown it can go positive.** Give every negative assertion a positive control in the
same run — `dev:sanitize`'s benign tail and `dev:roll-npc`'s accept-path are exactly this,
and they are why neither can be fooled by "the write never landed".

**A precondition a previous run left behind is not a precondition.** `dev:dialogs` set
`show-containers-tab`, slept 1s and clicked the tab — but that setting was registered
`requiresReload: true`, so it never appeared on an already-open sheet. It only ever worked
because an earlier run had left the setting on; the first run against a world where it was
off timed out, and every run after that passed. When a probe changes a setting, either
reload or check how that setting is registered. (Found 2026-07-29 by the "failed once,
passed on re-run" rule below. The setting itself was removed 2026-07-31 — the Connections
tab is structural — so the probes no longer arrange anything here; the rule is what
survives, not the example.)

**Two derived artifacts compared to each other cannot show a defect in what they
were derived from.** `i18n:check` compares `es.json` to `en.json`, which makes it
structurally blind to a string that was never localized at all — such a string is
identical in both files because it is in neither. That is how a hardcoded
`title="Double click to change limit"` shipped as the only hint its feature existed.
`i18n:source` closes it by comparing the **code** to `en.json`, and `dev:i18n-render`
closes the runtime half. The shape generalises: whenever a gate diffs two outputs of
the same pipeline, ask what a defect in the pipeline itself would look like to it.

**A probe with no `npm` script is a probe nobody runs — and it rots silently.**
`spellbook-prefix-probe.mjs` had none, and had decayed to selecting
`actor.sheet.element?.[0]`, an ApplicationV1 idiom that returns `undefined` on an
AppV2 sheet; every assertion read `null`. Its unique assertion was folded into
`dev:i18n-render` and the file deleted. **18 more probes under `tools/dev/` still
have no script** (`npm run` them by path to see which still work) — assume each has
rotted until it is run.

**A test that cannot fail is worse than none** — it reports success. When adding one,
confirm it fails with its fix removed. `dev:icon-canvas` is the pattern: it plants a
document holding the *old* state and watches it get rewritten, because asserting "nothing
is in the old state" passes trivially on an already-migrated world.

**…and prove that INSIDE the run, not by editing source and running again.** Stubbing the
fix in `module/`, relaunching, then restoring costs a browser launch per direction and
leaves the stub in the working tree if anything kills the run mid-flight. Both happened on
2026-07-29: ~11 minutes across two runs, one of which died to a hang and left
`if (true) return;` in `module/cairn.js`. In-page fault injection does the same job in
**seconds**, in the run you were making anyway:

| the fix is | switch it off with |
| --- | --- |
| a hook | `Hooks.off` — `Hooks.events` is a public `{hook, id, fn}` registry; `lib.mjs` `withHookOff(page, hook, fnName, fn)` wraps it, restoring in a Node-level `finally`. Register the handler as a **named** function expression so the probe can find it |
| a document override (`_preUpdate`, `prepareData`) | reassign the prototype method to the base class's for the duration |
| a CSS rule | walk `document.styleSheets`, blank the property, put it back |
| a translated string | `dev:i18n-render`'s trick — swap `game.i18n.translations` for a copy carrying unique **sentinels**, so a pre-fix code path emitting the English literal cannot match. Every assertion becomes its own negative control and no second run exists at all |

`dev:spellscroll` shows the shape: it asserts the Create-Item dialog offers "Spellscroll",
then switches the hook off, asserts the option is **gone**, and switches it back — 5.2s
inside an 80s run, versus 11 minutes and a dirty tree.

**A hang is the worst failure mode, and dialogs are how you get one.** `DialogV2` is modal
and its promise settles only on a button press, so a probe path that returns without
pressing anything waits forever: it burns the whole harness timeout and reports nothing.
Cancelling is not the escape either — a cancelled `DialogV2.prompt` *rejects*, so an
uncaught `await` throws past the assertions. Press a button on **every** path, `.catch()`
the promise, and call `lib.mjs` `watchdog(ms, label)` after launching the browser so any
future hang dies with a message instead of a timeout.

**When the defect is how much WORK happens, count it — an assertion on the output cannot
fail.** `findCompendiumItem` loaded a whole pack per lookup, so opening the shop did 78
full pack loads to resolve 77 items. The catalog was correct before and after; there was
no wrong value to assert on. `dev:compendium` instruments the call and discriminates on
the query (empty = a full pack load, `{_id}` = the cheap single fetch), which fails at 78
and passes at 1. The same shape fits any fix whose whole effect is cost: round-trips,
renders, listeners, document writes.

**Expect noise, not failures, from software rendering.** Headless Chromium here has no
GPU; the canvas runs on SwiftShader at roughly 3–4 FPS. `dev:smoke` filters Foundry's
hardware-acceleration warning and the viewport warning. Canvas probes are slow rather
than broken — `dev:icon-canvas` uses a known-good control token precisely so a rendering
hiccup reports as *inconclusive* rather than as a defect.

**`npm run lint` rewrites files.** It has `--fix` baked in, so it mutates `module/`
in place rather than reporting. Do not run it casually mid-change.
